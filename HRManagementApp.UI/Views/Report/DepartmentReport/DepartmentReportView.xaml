<UserControl x:Class="HRManagementApp.UI.Views.Report.DepartmentReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             mc:Ignorable="d"
             d:DesignWidth="1200"
             d:DesignHeight="800"
             Loaded="UserControl_Loaded">

    <!-- ====================== RESOURCES ====================== -->
    <UserControl.Resources>

        <!-- Dashboard Card -->
        <Style x:Key="DashboardCard"
               TargetType="Border">
            <Setter Property="Background"
                    Value="White"/>
            <Setter Property="CornerRadius"
                    Value="12"/>
            <Setter Property="Padding"
                    Value="20"/>
            <Setter Property="Margin"
                    Value="0,0,16,0"/>
            <Setter Property="BorderBrush"
                    Value="#E2E8F0"/>
            <Setter Property="BorderThickness"
                    Value="1"/>
        </Style>

        <!-- Card Title -->
        <Style x:Key="CardTitle"
               TargetType="TextBlock">
            <Setter Property="Foreground"
                    Value="#64748B"/>
            <Setter Property="FontSize"
                    Value="14"/>
            <Setter Property="FontWeight"
                    Value="Medium"/>
            <Setter Property="Margin"
                    Value="0,0,0,8"/>
        </Style>

        <!-- Card Value -->
        <Style x:Key="CardValue"
               TargetType="TextBlock">
            <Setter Property="Foreground"
                    Value="#1E293B"/>
            <Setter Property="FontSize"
                    Value="32"/>
            <Setter Property="FontWeight"
                    Value="Bold"/>
        </Style>

    </UserControl.Resources>

    <!-- ====================== ROOT GRID ====================== -->
    <Grid Background="White"
          Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="250"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ====================== TITLE ====================== -->
        <StackPanel Grid.Row="0"
                    Margin="0,0,0,24">
            <TextBlock Text="Báo Cáo Phòng Ban"
                       FontSize="24"
                       FontWeight="Bold"
                       Foreground="#1E293B"/>
            <TextBlock Text="Tổng quan phòng ban và phân bố nhân sự."
                       FontSize="14"
                       Foreground="#64748B"
                       Margin="0,4,0,0"/>
        </StackPanel>

        <!-- ====================== TOP AREA ====================== -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.3*"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <!-- TOTAL DEPARTMENTS CARD -->
            <Border Grid.Column="0"
                    Style="{StaticResource DashboardCard}">
                <StackPanel>
                    <Grid>
                        <TextBlock Text="Tổng số phòng ban"
                                   Style="{StaticResource CardTitle}"/>
                        <TextBlock Text="🏢"
                                   HorizontalAlignment="Right"
                                   FontSize="22"
                                   Opacity="0.5"/>
                    </Grid>
                    <TextBlock x:Name="TxtTotalDepartments"
                               Style="{StaticResource CardValue}"/>
                </StackPanel>
            </Border>

            <!-- BAR CHART RIGHT -->
            <Border Grid.Column="2"
                    Background="White"
                    CornerRadius="12"
                    Padding="20"
                    BorderBrush="#E2E8F0"
                    BorderThickness="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="Số lượng nhân viên từng phòng ban"
                               FontSize="16"
                               FontWeight="SemiBold"
                               Foreground="#1E293B"
                               Margin="0,0,0,16"/>

                    <lvc:CartesianChart Grid.Row="1"
                                        Series="{Binding DepartmentEmployeeSeries}"
                                        XAxes="{Binding DepartmentXAxes}"
                                        YAxes="{Binding DepartmentYAxes}"
                                        TooltipPosition="Top"/>
                </Grid>
            </Border>
        </Grid>

        <!-- ====================== SEARCH BAR ====================== -->
        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    VerticalAlignment="Center"
                    Margin="0,0,0,8"
                    HorizontalAlignment="Left">

            <TextBlock Text="Tìm phòng ban:"
                       FontSize="14"
                       FontWeight="SemiBold"
                       VerticalAlignment="Center"
                       Foreground="#1E293B"
                       Margin="0,0,12,0"/>

            <TextBox x:Name="txtSearchDepartment"
                     Width="260"
                     Height="32"
                     Padding="8,4"
                     FontSize="14"
                     VerticalContentAlignment="Center"
                     Background="White"
                     BorderBrush="#CBD5E1"
                     BorderThickness="1"
                     TextChanged="TxtSearchDepartment_TextChanged"
                     Text="Tìm phòng ban..."/>

            <!-- NÚT XUẤT EXCEL -->
            <Button x:Name="btnExportDepartment"
                    Content="Xuất Excel"
                    Width="110"
                    Height="32"
                    Margin="12,0,0,0"
                    Background="#3B82F6"
                    Foreground="White"
                    FontSize="14"
                    FontWeight="SemiBold"
                    BorderThickness="0"
                    Cursor="Hand"
                    Click="BtnExportDepartment_Click"/>
        </StackPanel>

        <!-- ====================== DEPARTMENT DATAGRID ====================== -->
        <DataGrid x:Name="DgDepartmentReport"
                  Grid.Row="5"
                  MinHeight="500"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  HeadersVisibility="Column"
                  AlternatingRowBackground="#F2F7FF"
                  GridLinesVisibility="None"
                  RowHeight="30"
                  FontSize="14"
                  ColumnHeaderHeight="40"
                  HorizontalGridLinesBrush="#E2E8F0"
                  VerticalGridLinesBrush="#E2E8F0">

            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="HorizontalContentAlignment"
                            Value="Left"/>
                    <Setter Property="FontWeight"
                            Value="SemiBold"/>
                    <Setter Property="Background"
                            Value="#EEF3FA"/>
                    <Setter Property="BorderBrush"
                            Value="#D0D7E1"/>
                    <Setter Property="BorderThickness"
                            Value="0 0 1 1"/>
                    <Setter Property="Padding"
                            Value="6 0"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>

            <!-- COLUMNS -->
            <DataGrid.Columns>

                <DataGridTextColumn Header="Mã PB"
                                    Width="*"
                                    Binding="{Binding MaPB}"/>

                <DataGridTextColumn Header="Tên phòng ban"
                                    Width="*"
                                    Binding="{Binding TenPB}"/>

                <DataGridTextColumn Header="Trưởng phòng"
                                    Width="*"
                                    Binding="{Binding TruongPhong}"/>

                <DataGridTextColumn Header="Số nhân viên"
                                    Width="*"
                                    Binding="{Binding SoNhanVien}"/>

            </DataGrid.Columns>
        </DataGrid>

    </Grid>

</UserControl>
