<Window x:Class="HRManagementApp.UI.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:HRManagementApp.UI"
        Title="Login"
        Height="400" Width="350"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{StaticResource BackgroundColor}">

    <Window.Resources>
        <SolidColorBrush x:Key="PrimaryColor" Color="#2E86AB"/>
        <SolidColorBrush x:Key="CardColor" Color="White"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#2C3E50"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#7F8C8D"/>
        <local:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </Window.Resources>

    <Grid>
        <Border Background="{StaticResource CardColor}" CornerRadius="8" Padding="30" Width="300" HorizontalAlignment="Center" VerticalAlignment="Center" >
            <StackPanel>
                <TextBlock Text="HR Management Login" FontSize="18" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" TextAlignment="Center"/>

                <!-- Username -->
                <Grid Margin="0,20,0,0">
                    <TextBox x:Name="UsernameTextBox" FontSize="14" Padding="10"/>
                    <TextBlock Text="Username" Foreground="Gray" Margin="12,0,0,0" VerticalAlignment="Center"
                               IsHitTestVisible="False"
                               Visibility="{Binding Text.IsEmpty, ElementName=UsernameTextBox, Converter={StaticResource BoolToVisibilityConverter}}"/>
                </Grid>

                <!-- Password -->
                <Grid Margin="0,10,0,0">
    <PasswordBox x:Name="PasswordBox"
                 FontSize="14" Padding="10"
                 PasswordChanged="PasswordBox_PasswordChanged"/>

    <TextBlock x:Name="PasswordPlaceholder"
               Text="Password"
               Foreground="Gray"
               Margin="12,0,0,0"
               VerticalAlignment="Center"
               IsHitTestVisible="False"/>
    
</Grid>


                <Button Content="Login" Background="{StaticResource PrimaryColor}" Foreground="White" Margin="0,20,0,0" Click="LoginButton_Click"/>
                <TextBlock x:Name="ErrorText" Foreground="Red" Margin="0,10,0,0" TextAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
