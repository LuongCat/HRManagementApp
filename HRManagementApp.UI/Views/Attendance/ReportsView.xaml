<UserControl x:Class="HRManagementApp.UI.Views.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="900"
             d:DesignWidth="1360">

    <Border Background="White"
            CornerRadius="0,12,12,12"
            Padding="32">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0"
                    Background="White"
                    BorderBrush="#E5E7EB"
                    BorderThickness="1"
                    CornerRadius="12"
                    Padding="20"
                    Margin="0,0,24,0"
                    VerticalAlignment="Top">
                <StackPanel Width="280">
                    <TextBlock Text="Chọn ngày xem"
                               FontSize="16"
                               FontWeight="SemiBold"
                               Foreground="#1E293B"
                               Margin="0,0,0,16"/>

                    <Calendar x:Name="ReportCalendar"
                              Background="White"
                              BorderThickness="0"
                              Width="260"
                              HorizontalAlignment="Center"
                              SelectedDatesChanged="ReportCalendar_SelectedDatesChanged"/>
                </StackPanel>
            </Border>

            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0">
                    <TextBlock x:Name="txtReportTitle"
                               Text="Báo cáo chấm công tháng ..."
                               FontSize="18"
                               FontWeight="SemiBold"
                               Foreground="#1E293B"
                               Margin="0,0,0,24"/>
                    <Button Content="📥 Xuất Excel"
                            HorizontalAlignment="Right"
                            Background="#10B981"
                            Foreground="White"
                            FontWeight="SemiBold"
                            Padding="16,8"
                            BorderThickness="0"
                            Cursor="Hand"
                            Click="BtnExport_Click">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius"
                                        Value="6"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                    <UniformGrid Columns="4"
                                 Rows="1"
                                 Margin="0,0,0,24">
                        <Border BorderBrush="#E5E7EB"
                                BorderThickness="1"
                                CornerRadius="12"
                                Padding="20"
                                Background="White"
                                Margin="0,0,12,0">
                            <StackPanel>
                                <TextBlock Text="Tổng số ngày công"
                                           Foreground="#64748B"
                                           FontSize="13"
                                           Margin="0,0,0,8"/>
                                <TextBlock x:Name="txtTotalDays"
                                           Text="0"
                                           FontWeight="Bold"
                                           FontSize="24"
                                           Foreground="#2563EB"/>
                            </StackPanel>
                        </Border>
                        <Border BorderBrush="#E5E7EB"
                                BorderThickness="1"
                                CornerRadius="12"
                                Padding="20"
                                Background="White"
                                Margin="0,0,12,0">
                            <StackPanel>
                                <TextBlock Text="Tỷ lệ đi làm"
                                           Foreground="#64748B"
                                           FontSize="13"
                                           Margin="0,0,0,8"/>
                                <TextBlock x:Name="txtRate"
                                           Text="0%"
                                           FontWeight="Bold"
                                           FontSize="24"
                                           Foreground="#16A34A"/>
                            </StackPanel>
                        </Border>
                        <Border BorderBrush="#E5E7EB"
                                BorderThickness="1"
                                CornerRadius="12"
                                Padding="20"
                                Background="White"
                                Margin="0,0,12,0">
                            <StackPanel>
                                <TextBlock Text="Tổng giờ làm"
                                           Foreground="#64748B"
                                           FontSize="13"
                                           Margin="0,0,0,8"/>
                                <TextBlock x:Name="txtTotalHours"
                                           Text="0"
                                           FontWeight="Bold"
                                           FontSize="24"
                                           Foreground="#2563EB"/>
                            </StackPanel>
                        </Border>
                        <Border BorderBrush="#E5E7EB"
                                BorderThickness="1"
                                CornerRadius="12"
                                Padding="20"
                                Background="White">
                            <StackPanel>
                                <TextBlock Text="Giờ tăng ca"
                                           Foreground="#64748B"
                                           FontSize="13"
                                           Margin="0,0,0,8"/>
                                <TextBlock x:Name="txtOvertime"
                                           Text="0"
                                           FontWeight="Bold"
                                           FontSize="24"
                                           Foreground="#F97316"/>
                            </StackPanel>
                        </Border>
                    </UniformGrid>
                </StackPanel>

                <Border Grid.Row="1"
                        BorderBrush="#E5E7EB"
                        BorderThickness="1"
                        CornerRadius="12"
                        Background="White"
                        Padding="0"
                        Margin="0,0,0,0">
                    <StackPanel>
                        <TextBlock x:Name="txtDetailTitle"
                                   Text="Chi tiết chấm công ngày ..."
                                   FontSize="16"
                                   FontWeight="SemiBold"
                                   Margin="20,20,20,16"
                                   Foreground="#1E293B"/>

                        <DataGrid x:Name="dgDailyDetail"
                                  AutoGenerateColumns="False"
                                  HeadersVisibility="Column"
                                  CanUserAddRows="False"
                                  GridLinesVisibility="None"
                                  BorderThickness="0"
                                  Background="White"
                                  RowBackground="White"
                                  AlternatingRowBackground="#F9FAFB"
                                  RowHeight="48"
                                  MaxHeight="400"
                                  Margin="20,0,20,20">
                            <DataGrid.Resources>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="Background"
                                            Value="Transparent"/>
                                    <Setter Property="Foreground"
                                            Value="#64748B"/>
                                    <Setter Property="FontWeight"
                                            Value="SemiBold"/>
                                    <Setter Property="Padding"
                                            Value="12,8"/>
                                    <Setter Property="BorderThickness"
                                            Value="0"/>
                                </Style>
                                <Style TargetType="DataGridCell">
                                    <Setter Property="BorderThickness"
                                            Value="0"/>
                                    <Setter Property="Padding"
                                            Value="12,0"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="DataGridCell">
                                                <Border Padding="{TemplateBinding Padding}"
                                                        Background="{TemplateBinding Background}">
                                                    <ContentPresenter VerticalAlignment="Center"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </DataGrid.Resources>

                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Mã NV"
                                                    Binding="{Binding Id}"
                                                    Width="80"/>
                                <DataGridTextColumn Header="Họ tên"
                                                    Binding="{Binding Name}"
                                                    Width="*"/>
                                <DataGridTextColumn Header="Phòng ban"
                                                    Binding="{Binding Department}"
                                                    Width="*"/>
                                <DataGridTextColumn Header="Giờ vào"
                                                    Binding="{Binding CheckIn}"
                                                    Width="80"/>
                                <DataGridTextColumn Header="Giờ ra"
                                                    Binding="{Binding CheckOut}"
                                                    Width="80"/>
                                <DataGridTextColumn Header="Số giờ"
                                                    Binding="{Binding WorkHours}"
                                                    Width="80"/>
                                <DataGridTextColumn Header="Trạng thái"
                                                    Binding="{Binding Status}"
                                                    Width="100"
                                                    FontWeight="Medium"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </Border>
</UserControl>