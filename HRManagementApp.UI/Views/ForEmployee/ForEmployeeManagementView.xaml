<UserControl x:Class="HRManagementApp.UI.Views.ForEmployeeManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="900"
             d:DesignWidth="1400">

    <UserControl.Resources>
        <Style x:Key="TabButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#E8E8E8"/>
            <Setter Property="Foreground" Value="#1E293B"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="Margin" Value="0,0,2,0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                               CornerRadius="12,12,0,0"
                               Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F0F0F0"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Background="#F5F5F5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="20">
            <TextBlock Text="Giao diện cho Nhân viên"
                       FontSize="28"
                       FontWeight="Bold"
                       Foreground="#1E293B"
                       Margin="0,0,0,8"/>
            <TextBlock Text="Cho Nhân viên chấm công và gửi đơn xin nghỉ"
                       FontSize="14"
                       Foreground="#64748B"
                       Margin="0,0,0,24"/>

            <StackPanel Orientation="Horizontal">
                <Button x:Name="btnAttendance"
                        Content="Chấm công "
                        Click="BtnAttendance_Click"
                        Style="{StaticResource TabButtonStyle}"/>
                <Button x:Name="btnLeave"
                        Content="Đơn xin nghỉ"
                        Click="BtnLeave_Click"
                        Style="{StaticResource TabButtonStyle}"/>
                
               
            </StackPanel>
        </StackPanel>

        <ContentControl x:Name="ContentArea" 
                       Grid.Row="1" 
                       Margin="20,0,20,20"/>
    </Grid>
</UserControl>