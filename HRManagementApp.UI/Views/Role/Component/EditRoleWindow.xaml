<Window x:Class="HRManagementApp.UI.Views.EditRoleWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Cập nhật vai trò"
        Height="420"
        Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F8FAFC">

    <Window.Resources>
        <Style x:Key="ModernTextBox"
                TargetType="TextBox">
            <Setter Property="Padding"
                    Value="12,10"/>
            <Setter Property="FontSize"
                    Value="14"/>
            <Setter Property="BorderBrush"
                    Value="#E2E8F0"/>
            <Setter Property="BorderThickness"
                    Value="1"/>
            <Setter Property="Background"
                    Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost"
                                    Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused"
                                    Value="True">
                                <Setter TargetName="border"
                                        Property="BorderBrush"
                                        Value="#F59E0B"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernButton"
                TargetType="Button">
            <Setter Property="Padding"
                    Value="0,10"/>
            <Setter Property="FontSize"
                    Value="14"/>
            <Setter Property="FontWeight"
                    Value="SemiBold"/>
            <Setter Property="Cursor"
                    Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center"
                                    VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver"
                                    Value="True">
                                <Setter TargetName="border"
                                        Property="Opacity"
                                        Value="0.9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Padding="24">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0"
                    Margin="0,0,0,24">
                <TextBlock Text="Cập Nhật Vai Trò"
                        FontSize="20"
                        FontWeight="Bold"
                        Foreground="#1E293B"/>
                <TextBlock Text="Chỉnh sửa thông tin vai trò hiện tại."
                        FontSize="13"
                        Foreground="#64748B"
                        Margin="0,4,0,0"/>
            </StackPanel>

            <StackPanel Grid.Row="1">
                <TextBlock Text="Tên vai trò"
                        FontSize="14"
                        FontWeight="SemiBold"
                        Foreground="#334155"
                        Margin="0,0,0,8"/>
                <TextBox x:Name="txtName"
                        Style="{StaticResource ModernTextBox}"
                        Margin="0,0,0,20"/>

                <TextBlock Text="Mô tả"
                        FontSize="14"
                        FontWeight="SemiBold"
                        Foreground="#334155"
                        Margin="0,0,0,8"/>
                <TextBox x:Name="txtDesc"
                        Style="{StaticResource ModernTextBox}"
                         Height="80"
                        TextWrapping="Wrap"
                        VerticalScrollBarVisibility="Auto"
                        AcceptsReturn="True"/>
            </StackPanel>

            <Grid Grid.Row="2"
                    Margin="0,24,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0"
                        Content="Hủy bỏ"
                        IsCancel="True"
                        Style="{StaticResource ModernButton}"
                        Background="#F1F5F9"
                        Foreground="#64748B"
                        BorderThickness="0"/>

                <Button Grid.Column="2"
                        Content="Lưu thay đổi"
                        Click="BtnSave_Click"
                        Style="{StaticResource ModernButton}"
                        Background="#F59E0B"
                        Foreground="White"
                        BorderThickness="0"/>
            </Grid>
        </Grid>
    </Border>
</Window>