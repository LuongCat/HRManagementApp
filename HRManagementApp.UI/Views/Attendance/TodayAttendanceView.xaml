<UserControl x:Class="HRManagementApp.UI.Views.TodayAttendanceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="800"
             d:DesignWidth="1360">

    <Border Background="White"
            CornerRadius="0,12,12,12"
            Padding="20">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel>

                <UniformGrid Columns="4" Margin="0,0,0,24">
                    <Border BorderBrush="#E5E7EB" BorderThickness="1" Padding="20" CornerRadius="12" Background="White" Margin="0,0,12,0">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Tổng số" Foreground="#64748B" FontSize="13" Margin="0,0,0,4"/>
                                <TextBlock x:Name="txtTotalStaff" 
                                           Text="0"
                                           FontSize="28"
                                           FontWeight="Bold"
                                           Foreground="#2563EB"/>
                            </StackPanel>
                            <Border Grid.Column="1" Width="48" Height="48" Background="#EFF6FF" CornerRadius="24" VerticalAlignment="Center">
                                <TextBlock Text="🕐" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </Grid>
                    </Border>

                    <Border BorderBrush="#E5E7EB" BorderThickness="1" Padding="20" CornerRadius="12" Background="White" Margin="0,0,12,0">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Đi làm" Foreground="#64748B" FontSize="13" Margin="0,0,0,4"/>
                                <TextBlock x:Name="txtPresent" 
                                           Text="0"
                                           FontSize="28"
                                           FontWeight="Bold"
                                           Foreground="#16A34A"/>
                            </StackPanel>
                            <Border Grid.Column="1" Width="48" Height="48" Background="#F0FDF4" CornerRadius="24" VerticalAlignment="Center">
                                <TextBlock Text="✓" FontSize="24" Foreground="#16A34A" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </Grid>
                    </Border>

                    <Border BorderBrush="#E5E7EB" BorderThickness="1" Padding="20" CornerRadius="12" Background="White" Margin="0,0,12,0">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Vắng mặt" Foreground="#64748B" FontSize="13" Margin="0,0,0,4"/>
                                <TextBlock x:Name="txtAbsent" 
                                           Text="0"
                                           FontSize="28"
                                           FontWeight="Bold"
                                           Foreground="#DC2626"/>
                            </StackPanel>
                            <Border Grid.Column="1" Width="48" Height="48" Background="#FEF2F2" CornerRadius="24" VerticalAlignment="Center">
                                <TextBlock Text="✕" FontSize="24" Foreground="#DC2626" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </Grid>
                    </Border>

                    <Border BorderBrush="#E5E7EB" BorderThickness="1" Padding="20" CornerRadius="12" Background="White">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Đi muộn" Foreground="#64748B" FontSize="13" Margin="0,0,0,4"/>
                                <TextBlock x:Name="txtLate" 
                                           Text="0"
                                           FontSize="28"
                                           FontWeight="Bold"
                                           Foreground="#F97316"/>
                            </StackPanel>
                            <Border Grid.Column="1" Width="48" Height="48" Background="#FFF7ED" CornerRadius="24" VerticalAlignment="Center">
                                <TextBlock Text="⚠" FontSize="24" Foreground="#F97316" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </Grid>
                    </Border>
                </UniformGrid>

                <Border BorderBrush="#E5E7EB" BorderThickness="1" CornerRadius="12" Background="White" Padding="0">
                    <StackPanel>
                        <TextBlock Text="Danh sách chấm công hôm nay" FontSize="16" FontWeight="SemiBold" Margin="20,20,20,16" Foreground="#1E293B"/>
                        
                        <DataGrid x:Name="dgTodayAttendance"
                                  AutoGenerateColumns="False"
                                  HeadersVisibility="Column"
                                  CanUserAddRows="False"
                                  GridLinesVisibility="None"
                                  BorderThickness="0"
                                  Background="White"
                                  RowBackground="White"
                                  AlternatingRowBackground="#F9FAFB"
                                  RowHeight="56"
                                  Margin="20,0,20,20">
                            <DataGrid.Resources>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="Foreground" Value="#64748B"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                    <Setter Property="FontSize" Value="13"/>
                                    <Setter Property="Padding" Value="12,8"/>
                                    <Setter Property="BorderThickness" Value="0"/>
                                </Style>
                                <Style TargetType="DataGridCell">
                                    <Setter Property="BorderThickness" Value="0"/>
                                    <Setter Property="Padding" Value="12,0"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="DataGridCell">
                                                <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}">
                                                    <ContentPresenter VerticalAlignment="Center"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </DataGrid.Resources>
                            
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Mã NV" Binding="{Binding Id}" Width="*" MinWidth="100"/>
                                <DataGridTextColumn Header="Họ tên" Binding="{Binding Name}" Width="*" MinWidth="150"/>
                                <DataGridTextColumn Header="Phòng ban" Binding="{Binding Department}" Width="*" MinWidth="120"/>
                                <DataGridTextColumn Header="Giờ vào" Binding="{Binding CheckIn}" Width="*" MinWidth="100"/>
                                <DataGridTextColumn Header="Giờ ra" Binding="{Binding CheckOut}" Width="*" MinWidth="100"/>
                                <DataGridTextColumn Header="Địa điểm" Binding="{Binding Location}" Width="*" MinWidth="150"/>
                                <DataGridTextColumn Header="Số giờ" Binding="{Binding WorkHours}" Width="*" MinWidth="100"/>
                                
                                <DataGridTemplateColumn Header="Trạng thái" Width="*" MinWidth="120">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Border CornerRadius="12" Padding="12,6" HorizontalAlignment="Left">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Status}" Value="Đúng giờ">
                                                                <Setter Property="Background" Value="#1E293B"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Status}" Value="Đi muộn">
                                                                <Setter Property="Background" Value="#F97316"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Status}" Value="Vắng">
                                                                <Setter Property="Background" Value="#DC2626"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Status}" Value="Đang làm">
                                                                <Setter Property="Background" Value="#64748B"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                                <TextBlock Text="{Binding Status}" Foreground="White" FontSize="12" FontWeight="Medium"/>
                                            </Border>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Border>
</UserControl>