<UserControl x:Class="HRManagementApp.UI.Views.DepartmentListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters1="clr-namespace:HRManagementApp.Converters;assembly=HRManagementApp.Models"
             xmlns:constants1="clr-namespace:HRManagementApp.Constants;assembly=HRManagementApp.Models"
             Background="Transparent">

    <UserControl.Resources>
        <converters1:PermissionToBooleanConverter x:Key="PermToBool"/>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <ItemsControl x:Name="DepartmentItemsControl">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Background="White" CornerRadius="12" 
                            Margin="0,0,20,20" Padding="25"
                            Width="350" MinHeight="280">

                        <Border.Effect>
                            <DropShadowEffect Color="#E0E0E0" BlurRadius="10" ShadowDepth="2" Opacity="0.3"/>
                        </Border.Effect>

                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Text="{Binding TenPhongBan}" 
                                       FontSize="20" FontWeight="Bold" Foreground="#1F2937"
                                       Margin="0,0,0,15"/>

                            <StackPanel Grid.Row="1" Margin="0,0,0,12">
                                <TextBlock Text="Trưởng phòng" FontSize="13" Foreground="#6B7280" Margin="0,0,0,4"/>
                                <TextBlock Text="{Binding TruongPhong}" FontSize="16" FontWeight="SemiBold" Foreground="#1F2937"/>
                            </StackPanel>

                            <StackPanel Grid.Row="2" Margin="0,0,0,12">
                                <TextBlock Text="Số nhân viên" FontSize="13" Foreground="#6B7280" Margin="0,0,0,4"/>
                                <TextBlock FontSize="16" Foreground="#2563EB" FontWeight="SemiBold">
                                    <Run Text="{Binding SoNhanVien}"/>
                                    <Run Text=" người"/>
                                </TextBlock>
                            </StackPanel>

                            <StackPanel Grid.Row="3" Margin="0,0,0,15">
                                <TextBlock Text="Mô tả" FontSize="13" Foreground="#6B7280" Margin="0,0,0,4"/>
                                <TextBlock Text="{Binding MoTa}" 
                                           FontSize="14" Foreground="#4B5563" 
                                           TextWrapping="Wrap" LineHeight="20"/>
                            </StackPanel>

                            <Grid Grid.Row="4"/>

                            <Grid Grid.Row="5" VerticalAlignment="Bottom">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="10"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <Button Grid.Row="0" Content="+ Thêm nhân viên"
                                        Background="#10B981" Foreground="White"
                                        BorderThickness="0"
                                        Padding="15,10" FontSize="14" FontWeight="SemiBold"
                                        Cursor="Hand"
                                        Tag="{Binding MaPhongBan}"
                                        Click="BtnAddEmployee_Click"
                                        IsEnabled="{Binding Converter={StaticResource PermToBool}, ConverterParameter={x:Static constants1:AppPermissions.PHONGBAN_SUA}}">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border Background="{TemplateBinding Background}" 
                                                                CornerRadius="8" 
                                                                Padding="{TemplateBinding Padding}">
                                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                        </Border>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#059669"/>
                                                </Trigger>
                                                <Trigger Property="IsEnabled" Value="False">
                                                    <Setter Property="Opacity" Value="0.5"/>
                                                    <Setter Property="Background" Value="#9CA3AF"/>
                                                    <Setter Property="Cursor" Value="No"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>

                                <Grid Grid.Row="2">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="8"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <Button Grid.Column="0" Content="✏️ Sửa"
                                            Background="White" Foreground="#1F2937"
                                            BorderBrush="#E5E7EB" BorderThickness="1"
                                            Padding="15,10" FontSize="14" FontWeight="SemiBold"
                                            Cursor="Hand"
                                            Tag="{Binding MaPhongBan}"
                                            Click="BtnEdit_Click"
                                            IsEnabled="{Binding Converter={StaticResource PermToBool}, ConverterParameter={x:Static constants1:AppPermissions.PHONGBAN_SUA}}">
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="Button">
                                                            <Border Background="{TemplateBinding Background}" 
                                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                                    CornerRadius="8" 
                                                                    Padding="{TemplateBinding Padding}">
                                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                            </Border>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="#F9FAFB"/>
                                                    </Trigger>
                                                    <Trigger Property="IsEnabled" Value="False">
                                                        <Setter Property="Opacity" Value="0.4"/>
                                                        <Setter Property="Cursor" Value="Arrow"/>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>

                                    <Button Grid.Column="2" Content="👁 Xem"
                                            Background="White" Foreground="#1F2937"
                                            BorderBrush="#E5E7EB" BorderThickness="1"
                                            Padding="15,10" FontSize="14" FontWeight="SemiBold"
                                            Cursor="Hand"
                                            Tag="{Binding MaPhongBan}"
                                            Click="BtnView_Click"
                                            IsEnabled="{Binding Converter={StaticResource PermToBool}, ConverterParameter={x:Static constants1:AppPermissions.PHONGBAN_XEM}}">
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="Button">
                                                            <Border Background="{TemplateBinding Background}" 
                                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                                    CornerRadius="8"
                                                                    Padding="{TemplateBinding Padding}">
                                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                            </Border>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="#F9FAFB"/>
                                                    </Trigger>
                                                    <Trigger Property="IsEnabled" Value="False">
                                                        <Setter Property="Opacity" Value="0.4"/>
                                                        <Setter Property="Cursor" Value="Arrow"/>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>
                                </Grid>
                            </Grid>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </ScrollViewer>
</UserControl>