<UserControl x:Class="HRManagementApp.UI.Views.PayrollView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="#F3F4F6">

    <Grid Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <RowDefinition Height="Auto"/> <RowDefinition Height="*"/>    </Grid.RowDefinitions>

        <TextBlock x:Name="HeaderTitle"
                   Grid.Row="0"
                   Text="Quản lý Bảng lương"
                   FontSize="24"
                   FontWeight="Bold"
                   Foreground="#1F2937"
                   Margin="0,0,0,20"/>

        <Border Grid.Row="1" 
                Background="White" 
                CornerRadius="8" 
                Padding="15" 
                Margin="0,0,0,15"
                BorderBrush="#E5E7EB" BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/> <ColumnDefinition Width="*"/>    <ColumnDefinition Width="Auto"/> <ColumnDefinition Width="Auto"/> </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Tháng:" VerticalAlignment="Center" Foreground="#6B7280" Margin="0,0,5,0"/>
                    <TextBox x:Name="txtMonth" Width="40" Height="32" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" BorderBrush="#D1D5DB" BorderThickness="1" FontSize="14"/>

                    <TextBlock Text="/" VerticalAlignment="Center" Foreground="#6B7280" Margin="5,0,5,0" FontWeight="Bold"/>

                    <TextBox x:Name="txtYear" Width="60" Height="32" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" BorderBrush="#D1D5DB" BorderThickness="1" FontSize="14"/>

                    <Button Content="Xem" Click="LoadData_Click" Margin="10,0,0,0" Background="#3B82F6" Foreground="White" BorderThickness="0" Padding="15,0" Height="32" Cursor="Hand" FontWeight="SemiBold">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="4"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>

                <Border Grid.Column="2" Width="250" Height="35" Background="#F9FAFB" CornerRadius="6" BorderBrush="#D1D5DB" BorderThickness="1" Margin="0,0,15,0">
                    <Grid>
                        <TextBlock Text="🔍 Tìm theo tên..." VerticalAlignment="Center" Margin="10,0,0,0" Foreground="#9CA3AF" IsHitTestVisible="False">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Text, ElementName=txtSearch}" Value="">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                        <TextBox x:Name="txtSearch" TextChanged="txtSearch_TextChanged" Background="Transparent" BorderThickness="0" VerticalContentAlignment="Center" Padding="10,0" FontSize="14"/>
                    </Grid>
                </Border>

                <Button Grid.Column="3" Content="⬇ Xuất Excel" Background="#1F2937" Foreground="White" BorderThickness="0" Padding="15,0" Height="35" Cursor="Hand" FontWeight="SemiBold" Click="ExportExcel_Click">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="6"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </Grid>
        </Border>

        <Border Grid.Row="2" Background="White" CornerRadius="8" BorderBrush="#E5E7EB" BorderThickness="1" ClipToBounds="True">
            <DataGrid x:Name="PayrollDataGrid"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      SelectionMode="Single"
                      RowHeight="45"
                      FontSize="13"
                      GridLinesVisibility="Horizontal"
                      HeadersVisibility="Column"
                      BorderThickness="0"
                      Background="White"
                      AlternatingRowBackground="#F9FAFB"
                      HorizontalScrollBarVisibility="Auto">

                <DataGrid.Resources>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#F3F4F6"/>
                        <Setter Property="Foreground" Value="#374151"/>
                        <Setter Property="FontSize" Value="13"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="Height" Value="45"/>
                        <Setter Property="HorizontalContentAlignment" Value="Center"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="#E5E7EB"/>
                    </Style>
                    
                    <Style TargetType="DataGridCell">
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Padding" Value="5,0"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="DataGridCell">
                                    <Border Background="{TemplateBinding Background}" BorderThickness="0">
                                        <ContentPresenter VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </DataGrid.Resources>

                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="Mã NV" Width="70">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding maNV}" FontWeight="Bold" Foreground="#4B5563" HorizontalAlignment="Center"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="Họ tên" Width="150">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding TenNV}" FontWeight="SemiBold" Foreground="#1F2937" TextTrimming="CharacterEllipsis"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="Lương CB" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding LuongCoBan, StringFormat=N0}" TextAlignment="Right"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="Hệ số" Width="60">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding HeSoLuongCB, StringFormat=N2}" TextAlignment="Center"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="Công" Width="60">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding TongNgayCong}" TextAlignment="Center"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="Phụ cấp" Width="90">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding TongPhuCap, StringFormat=N0}" TextAlignment="Right" Foreground="#059669"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="Kiêm nhiệm" Width="90">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding TongTienKiemNhiem, StringFormat=N0}" TextAlignment="Right" Foreground="#2563EB"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="Thuế" Width="85">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding TongThue, StringFormat=N0}" TextAlignment="Right" Foreground="#D97706"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="Khấu trừ" Width="90">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding TongKhauTru, StringFormat=N0}" TextAlignment="Right" Foreground="#DC2626"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="Thực nhận" Width="110">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding LuongThucNhan, StringFormat=N0}" TextAlignment="Right" FontWeight="Bold" Foreground="#111827"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="Trạng thái" Width="120">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border x:Name="StatusBorder" CornerRadius="12" Padding="8,2" HorizontalAlignment="Center" Background="#E5E7EB">
                                    <TextBlock x:Name="StatusText" Text="{Binding TrangThai}" FontSize="11" FontWeight="SemiBold" Foreground="#374151"/>
                                </Border>
                                <DataTemplate.Triggers>
                                    <DataTrigger Binding="{Binding TrangThai}" Value="Đã thanh toán">
                                        <Setter TargetName="StatusBorder" Property="Background" Value="#D1FAE5"/>
                                        <Setter TargetName="StatusText" Property="Foreground" Value="#065F46"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding TrangThai}" Value="Đã trả">
                                        <Setter TargetName="StatusBorder" Property="Background" Value="#D1FAE5"/>
                                        <Setter TargetName="StatusText" Property="Foreground" Value="#065F46"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding TrangThai}" Value="Chưa thanh toán">
                                        <Setter TargetName="StatusBorder" Property="Background" Value="#FEE2E2"/>
                                        <Setter TargetName="StatusText" Property="Foreground" Value="#991B1B"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding TrangThai}" Value="Chưa trả">
                                        <Setter TargetName="StatusBorder" Property="Background" Value="#FEE2E2"/>
                                        <Setter TargetName="StatusText" Property="Foreground" Value="#991B1B"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding TrangThai}" Value="Tạm giữ">
                                        <Setter TargetName="StatusBorder" Property="Background" Value="#FEF3C7"/>
                                        <Setter TargetName="StatusText" Property="Foreground" Value="#92400E"/>
                                    </DataTrigger>
                                     <DataTrigger Binding="{Binding TrangThai}" Value="Đã chốt">
                                        <Setter TargetName="StatusBorder" Property="Background" Value="#DBEAFE"/>
                                        <Setter TargetName="StatusText" Property="Foreground" Value="#1E40AF"/>
                                    </DataTrigger>
                                </DataTemplate.Triggers>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="Thao tác" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" >
                                    <Button Click="ViewDetail_Click" ToolTip="Xem chi tiết"
                                            Background="Transparent" BorderThickness="0" Cursor="Hand" Margin="0,0,5,0">
                                        <TextBlock Text="📄" FontSize="16" Foreground="#6B7280"/>
                                    </Button>

                                    <Button Click="ConfirmSalary_Click" ToolTip="Chốt lương"
                                            Background="Transparent" BorderThickness="0" Cursor="Hand" Margin="0,0,5,0">
                                        <TextBlock Text="🔒" FontSize="16" Foreground="#DC2626"/>
                                    </Button>

                                    <Button Click="ChangeStatusSalary_Click" ToolTip="Thanh toán"
                                            Background="Transparent" BorderThickness="0" Cursor="Hand">
                                        <TextBlock Text="💸" FontSize="16" Foreground="#059669"/>
                                    </Button>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>