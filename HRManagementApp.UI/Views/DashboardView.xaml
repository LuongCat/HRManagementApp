<UserControl x:Class="HRManagementApp.UI.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200"
             FontFamily="Segoe UI">

    <UserControl.Resources>
        <SolidColorBrush x:Key="PrimaryColor" Color="#3498DB"/>       <SolidColorBrush x:Key="SuccessColor" Color="#2ECC71"/>       <SolidColorBrush x:Key="WarningColor" Color="#F1C40F"/>       <SolidColorBrush x:Key="DangerColor" Color="#E74C3C"/>        <SolidColorBrush x:Key="BackgroundColor" Color="#F4F6F9"/>    <SolidColorBrush x:Key="CardColor" Color="White"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#2C3E50"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#7F8C8D"/>
        <SolidColorBrush x:Key="BorderColor" Color="#E0E0E0"/>

        <Style x:Key="ModernDataGridStyle" TargetType="DataGrid">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="RowHeight" Value="45"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#F0F0F0"/>
            <Setter Property="VerticalGridLinesBrush" Value="Transparent"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserResizeRows" Value="False"/>
        </Style>

        <Style x:Key="ModernColumnHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#FAFAFA"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="10,12"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
        </Style>

        <Style x:Key="ModernCellStyle" TargetType="DataGridCell">
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Padding="{TemplateBinding Padding}" 
                                Background="Transparent"
                                BorderBrush="Transparent" 
                                BorderThickness="0">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Foreground" Value="Black"/> <Setter Property="Background" Value="#EBF5FB"/> </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#F5F5F5"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F8F9FA"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource CardColor}"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Margin" Value="0,0,15,15"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000000" Direction="270" ShadowDepth="1" BlurRadius="10" Opacity="0.05"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="FilterComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Height" Value="32"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="Margin" Value="0,0,15,0"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
        </Style>

    </UserControl.Resources>

    <Grid Background="{StaticResource BackgroundColor}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <RowDefinition Height="Auto"/> <RowDefinition Height="*"/>    </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="White" Padding="30,20" Margin="0,0,0,20" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <Grid>
                <StackPanel>
                    <TextBlock Text="Overview Dashboard" FontSize="26" FontWeight="Bold" Foreground="{StaticResource TextPrimary}"/>
                    <TextBlock x:Name="TxtToday" Text="Today: September 20, 2025" FontSize="14" Foreground="{StaticResource TextSecondary}" Margin="0,5,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="1" Margin="30,0,15,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource CardStyle}" Padding="20">
                <StackPanel >
                    <Grid>
                        <Ellipse Width="40" Height="40" Fill="#EBF5FB" HorizontalAlignment="Left"/>
                        <TextBlock Text="👥" FontSize="20" HorizontalAlignment="Left" Margin="10,0,0,0" VerticalAlignment="Center"/>
                    </Grid>
                    <TextBlock x:Name="TxtTotalEmployee" Text="150" FontSize="28" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" Margin="0,15,0,0"/>
                    <TextBlock Text="Total Employees" FontSize="13" Foreground="{StaticResource TextSecondary}"/>
                    <TextBlock x:Name="TxtJoinedThisMonth" Text="+5 new" FontSize="12" Foreground="{StaticResource SuccessColor}" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Style="{StaticResource CardStyle}" Padding="20">
                <StackPanel >
                    <Grid>
                        <Ellipse Width="40" Height="40" Fill="#E8F8F5" HorizontalAlignment="Left"/>
                        <TextBlock Text="✅" FontSize="20" HorizontalAlignment="Left" Margin="10,0,0,0" VerticalAlignment="Center"/>
                    </Grid>
                    <TextBlock x:Name="TxtPresentToday" Text="142" FontSize="28" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" Margin="0,15,0,0"/>
                    <TextBlock Text="Present Today" FontSize="13" Foreground="{StaticResource TextSecondary}"/>
                    <TextBlock x:Name="TxtAttendanceRate" Text="96% Rate" FontSize="12" Foreground="{StaticResource SuccessColor}" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource CardStyle}" Padding="20">
                <StackPanel >
                    <Grid>
                        <Ellipse Width="40" Height="40" Fill="#FDEDEC" HorizontalAlignment="Left"/>
                        <TextBlock Text="🏖️" FontSize="20" HorizontalAlignment="Left" Margin="10,0,0,0" VerticalAlignment="Center"/>
                    </Grid>
                    <TextBlock x:Name="TxtOnLeave" Text="8" FontSize="28" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" Margin="0,15,0,0"/>
                    <TextBlock Text="On Leave" FontSize="13" Foreground="{StaticResource TextSecondary}"/>
                    <TextBlock x:Name="TxtPendingApprovals" Text="2 Pending" FontSize="12" Foreground="{StaticResource WarningColor}" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="3" Style="{StaticResource CardStyle}" Margin="0,0,0,15" Padding="20">
                <StackPanel >
                    <Grid>
                        <Ellipse Width="40" Height="40" Fill="#FEF9E7" HorizontalAlignment="Left"/>
                        <TextBlock Text="💰" FontSize="20" HorizontalAlignment="Left" Margin="10,0,0,0" VerticalAlignment="Center"/>
                    </Grid>
                    <TextBlock x:Name="TxtMonthlyPayroll" Text="$420k" FontSize="28" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" Margin="0,15,0,0"/>
                    <TextBlock Text="Monthly Payroll" FontSize="13" Foreground="{StaticResource TextSecondary}"/>
                    <TextBlock x:Name="TxtDueDate" Text="Due in 5 days" FontSize="12" Foreground="{StaticResource DangerColor}" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>
        </Grid>

        <Border Grid.Row="2" Style="{StaticResource CardStyle}" Margin="30,0,30,30" Padding="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/> <RowDefinition Height="*"/>    </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/> <ColumnDefinition Width="*"/>    <ColumnDefinition Width="Auto"/> </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="System Activity Logs" FontSize="18" FontWeight="Bold" Foreground="{StaticResource TextPrimary}"/>
                        <TextBlock Text="Monitor all system changes and user actions" FontSize="12" Foreground="{StaticResource TextSecondary}" Margin="0,5,0,0"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                        <ComboBox x:Name="cboFilterMonth" Style="{StaticResource FilterComboBoxStyle}" Width="90" SelectionChanged="Filter_Changed" SelectedIndex="0">
                            <ComboBoxItem Content="Month: All"/>
                            <ComboBoxItem Content="1"/>
                            <ComboBoxItem Content="2"/>
                            <ComboBoxItem Content="3"/>
                            <ComboBoxItem Content="4"/>
                            <ComboBoxItem Content="5"/>
                            <ComboBoxItem Content="6"/>
                            <ComboBoxItem Content="7"/>
                            <ComboBoxItem Content="8"/>
                            <ComboBoxItem Content="9"/>
                            <ComboBoxItem Content="10"/>
                            <ComboBoxItem Content="11"/>
                            <ComboBoxItem Content="12"/>
                        </ComboBox>

                        <ComboBox x:Name="cboFilterYear" Style="{StaticResource FilterComboBoxStyle}" Width="90" SelectionChanged="Filter_Changed"/>

                        <ComboBox x:Name="cboFilterAction" Style="{StaticResource FilterComboBoxStyle}" Width="110" SelectionChanged="Filter_Changed" SelectedIndex="0">
                            <ComboBoxItem Content="Action: All"/>
                            <ComboBoxItem Content="INSERT"/>
                            <ComboBoxItem Content="UPDATE"/>
                            <ComboBoxItem Content="DELETE"/>
                            <ComboBoxItem Content="LOGIN"/>
                            <ComboBoxItem Content="CHECK_IN"/>
                            <ComboBoxItem Content="CHECK_OUT"/>
                        </ComboBox>

                        <ComboBox x:Name="cboFilterTable" Style="{StaticResource FilterComboBoxStyle}" Width="110" SelectionChanged="Filter_Changed" SelectedIndex="0">
                            <ComboBoxItem Content="Table: All"/>
                            <ComboBoxItem Content="NhanVien"/>
                            <ComboBoxItem Content="ChamCong"/>
                            <ComboBoxItem Content="TaiKhoan"/>
                        </ComboBox>

                        <Button Click="BtnRefresh_Click" Background="Transparent" BorderThickness="0" Cursor="Hand" ToolTip="Refresh Data">
                            <Border Background="#F4F6F9" CornerRadius="4" Padding="10">
                                <TextBlock Text="🔄" FontSize="14"/>
                            </Border>
                        </Button>
                    </StackPanel>
                </Grid>

                <DataGrid Grid.Row="1" x:Name="dgSystemLogs"
                          Style="{StaticResource ModernDataGridStyle}"
                          ColumnHeaderStyle="{StaticResource ModernColumnHeaderStyle}"
                          CellStyle="{StaticResource ModernCellStyle}">
                    
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="TIME" Binding="{Binding ThoiGian, StringFormat='dd/MM/yyyy HH:mm'}" Width="150" Foreground="{StaticResource TextSecondary}"/>

                        <DataGridTemplateColumn Header="USER" Width="180">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <Border Width="24" Height="24" CornerRadius="12" Background="#D6EAF8" Margin="0,0,10,0">
                                            <TextBlock Text="👤" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="10"/>
                                        </Border>
                                        <TextBlock Text="{Binding NguoiThucHien}" FontWeight="SemiBold" VerticalAlignment="Center" Foreground="{StaticResource TextPrimary}"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTemplateColumn Header="ACTION" Width="140">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border CornerRadius="12" Padding="12,4" HorizontalAlignment="Left" VerticalAlignment="Center">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="#F0F0F0"/> <Style.Triggers>
                                                    <DataTrigger Binding="{Binding HanhDong}" Value="INSERT">
                                                        <Setter Property="Background" Value="#E8F8F5"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding HanhDong}" Value="CHECK_IN">
                                                        <Setter Property="Background" Value="#E8F8F5"/>
                                                    </DataTrigger>
                                                    
                                                    <DataTrigger Binding="{Binding HanhDong}" Value="UPDATE">
                                                        <Setter Property="Background" Value="#FEF9E7"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding HanhDong}" Value="CHECK_OUT">
                                                        <Setter Property="Background" Value="#FEF9E7"/>
                                                    </DataTrigger>
                                                    
                                                    <DataTrigger Binding="{Binding HanhDong}" Value="DELETE">
                                                        <Setter Property="Background" Value="#FDEDEC"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        
                                        <TextBlock Text="{Binding HanhDong}" FontWeight="Bold" FontSize="11">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="#7F8C8D"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding HanhDong}" Value="INSERT">
                                                            <Setter Property="Foreground" Value="#27AE60"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding HanhDong}" Value="CHECK_IN">
                                                            <Setter Property="Foreground" Value="#27AE60"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding HanhDong}" Value="UPDATE">
                                                            <Setter Property="Foreground" Value="#F39C12"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding HanhDong}" Value="CHECK_OUT">
                                                            <Setter Property="Foreground" Value="#F39C12"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding HanhDong}" Value="DELETE">
                                                            <Setter Property="Foreground" Value="#E74C3C"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTextColumn Header="TABLE" Binding="{Binding BangLienQuan}" Width="120" Foreground="{StaticResource TextSecondary}"/>

                        <DataGridTemplateColumn Header="DESCRIPTION" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding MoTa}" TextTrimming="CharacterEllipsis" ToolTip="{Binding MoTa}" VerticalAlignment="Center" Foreground="{StaticResource TextPrimary}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>