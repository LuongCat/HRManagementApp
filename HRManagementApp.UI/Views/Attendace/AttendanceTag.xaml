<UserControl x:Class="HRManagementApp.UI.Views.AttendanceTag"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#F5F7FA" Height="Auto" Width="Auto">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <RowDefinition Height="Auto"/> <RowDefinition Height="*"/>    </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="Quản lý Chấm công" 
                       FontSize="24" FontWeight="Bold" Foreground="#1F2937"/>
            <TextBlock Text="Theo dõi và quản lý thời gian làm việc của nhân viên" 
                       FontSize="14" Foreground="#6B7280" Margin="0,5,0,0"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,20">
            <StackPanel.Resources>
                <Style TargetType="RadioButton" x:Key="TabButtonStyle">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="#4B5563"/>
                    <Setter Property="FontSize" Value="14"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="Padding" Value="15,8"/>
                    <Setter Property="Margin" Value="0,0,10,0"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="RadioButton">
                                <Border x:Name="border" 
                                        Background="{TemplateBinding Background}" 
                                        CornerRadius="20" 
                                        Padding="{TemplateBinding Padding}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsChecked" Value="True">
                                        <Setter TargetName="border" Property="Background" Value="#F3F4F6"/>
                                        <Setter Property="Foreground" Value="#111827"/>
                                        <Setter Property="FontWeight" Value="Bold"/>
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="border" Property="Background" Value="#FFFFFF"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </StackPanel.Resources>

            <RadioButton Content="Chấm công hôm nay" GroupName="AttTabs" IsChecked="True" Style="{StaticResource TabButtonStyle}" Click="Tab_Click"/>
            <RadioButton Content="Công của nhân viên" GroupName="AttTabs" Style="{StaticResource TabButtonStyle}" Click="Tab_Click"/>
            
        </StackPanel>

        <ContentControl x:Name="MainContent" Grid.Row="2"/>
    </Grid>
</UserControl>