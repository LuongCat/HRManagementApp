<UserControl x:Class="HRManagementApp.UI.Views.ReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="800"
             d:DesignWidth="1200">

    <UserControl.Resources>
        <SolidColorBrush x:Key="PrimaryColor"
                         Color="#2563EB"/>
        <SolidColorBrush x:Key="PrimaryLight"
                         Color="#EFF6FF"/>
        <SolidColorBrush x:Key="TextPrimary"
                         Color="#1E293B"/>
        <SolidColorBrush x:Key="TextSecondary"
                         Color="#64748B"/>
        <SolidColorBrush x:Key="BorderColor"
                         Color="#E2E8F0"/>
        <SolidColorBrush x:Key="BgColor"
                         Color="#F8FAFC"/>
        <Style x:Key="TabButtonStyle"
               TargetType="ToggleButton">
            <Setter Property="Background"
                    Value="Transparent"/>
            <Setter Property="Foreground"
                    Value="{StaticResource TextSecondary}"/>
            <Setter Property="BorderThickness"
                    Value="0"/>
            <Setter Property="Padding"
                    Value="24,12"/>
            <Setter Property="FontSize"
                    Value="14"/>
            <Setter Property="FontWeight"
                    Value="SemiBold"/>
            <Setter Property="Cursor"
                    Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}"
                                Margin="0,0,8,0">
                            <StackPanel Orientation="Horizontal"
                                        HorizontalAlignment="Center">
                                <ContentPresenter VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver"
                                     Value="True">
                                <Setter TargetName="border"
                                        Property="Background"
                                        Value="White"/>
                                <Setter Property="Foreground"
                                        Value="{StaticResource PrimaryColor}"/>
                            </Trigger>
                            <Trigger Property="IsChecked"
                                     Value="True">
                                <Setter TargetName="border"
                                        Property="Background"
                                        Value="{StaticResource PrimaryColor}"/>
                                <Setter Property="Foreground"
                                        Value="White"/>
                                <Setter TargetName="border"
                                        Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#2563EB"
                                                          Direction="270"
                                                          ShadowDepth="2"
                                                          BlurRadius="8"
                                                          Opacity="0.3"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="{StaticResource BgColor}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Padding="32,32,32,20"
                Background="Transparent">
            <StackPanel>
                <TextBlock Text="Báo cáo &amp; Thống kê"
                           FontSize="28"
                           FontWeight="Bold"
                           Foreground="{StaticResource TextPrimary}"/>
                <TextBlock Text="Xem tổng quan, xuất báo cáo nhân sự, lương và chấm công."
                           FontSize="15"
                           Foreground="{StaticResource TextSecondary}"
                           Margin="0,6,0,0"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="1"
                Padding="32,0,32,24">
            <Grid>
                <Border Background="White"
                        CornerRadius="12"
                        Margin="2">
                    <Border.Effect>
                        <DropShadowEffect Color="Gray"
                                          Direction="270"
                                          ShadowDepth="1"
                                          BlurRadius="8"
                                          Opacity="0.1"/>
                    </Border.Effect>
                </Border>

                <Border Background="White"
                        CornerRadius="12"
                        Padding="8"
                        BorderBrush="{StaticResource BorderColor}"
                        BorderThickness="1">
                    <StackPanel x:Name="ReportMenuPanel"
                                Orientation="Horizontal">
                        <ToggleButton x:Name="DefaultReport"
                                      Content="📊 Tổng quan"
                                      Tag="Analytics"
                                      Checked="ReportMenu_Checked"
                                      Style="{StaticResource TabButtonStyle}"/>
                        <ToggleButton Content="📅 Chấm công"
                                      Tag="Attendance"
                                      Checked="ReportMenu_Checked"
                                      Style="{StaticResource TabButtonStyle}"/>
                        <ToggleButton Content="💰 Lương thưởng"
                                      Tag="Payroll"
                                      Checked="ReportMenu_Checked"
                                      Style="{StaticResource TabButtonStyle}"/>
                        <ToggleButton Content="📝 Nghỉ phép"
                                      Tag="Leave"
                                      Checked="ReportMenu_Checked"
                                      Style="{StaticResource TabButtonStyle}"/>
                        <ToggleButton Content="👥 Nhân viên"
                                      Tag="Employee"
                                      Checked="ReportMenu_Checked"
                                      Style="{StaticResource TabButtonStyle}"/>
                        <ToggleButton Content="🏢 Phòng ban"
                                      Tag="Department"
                                      Checked="ReportMenu_Checked"
                                      Style="{StaticResource TabButtonStyle}"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <Grid Grid.Row="2"
                Margin="32,0,32,32">
            <Border Background="White"
                    CornerRadius="16"
                    Margin="2">
                <Border.Effect>
                    <DropShadowEffect Color="Black"
                            Direction="270"
                            ShadowDepth="2"
                            BlurRadius="12"
                            Opacity="0.05"/>
                </Border.Effect>
            </Border>

            <Border Background="White"
                    CornerRadius="16"
                    BorderBrush="{StaticResource BorderColor}"
                    BorderThickness="1">

                <ContentControl x:Name="ReportContentArea"
                                Margin="0"
                                ClipToBounds="True"/>
            </Border>
        </Grid>

    </Grid>
</UserControl>