<UserControl x:Class="HRManagementApp.UI.Views.MyPayslipView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="850" d:DesignWidth="900"
             Background="#F3F4F6">

    <UserControl.Resources>
        <Style x:Key="SectionHeader" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#374151"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>

        <Style x:Key="LabelText" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#6B7280"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="ValueText" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#111827"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <RowDefinition Height="Auto"/> </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="Phiếu Lương Cá Nhân" FontSize="28" FontWeight="Bold" Foreground="#1F2937"/>
                    <TextBlock Text="Xem chi tiết thu nhập và khấu trừ hàng tháng" FontSize="14" Foreground="#6B7280" Margin="0,5,0,0"/>
                </StackPanel>
                
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <TextBlock Text="Chọn kỳ lương:" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="#4B5563"/>
                    <ComboBox x:Name="cboMonth" Width="180" Height="35" 
                              Padding="10,0" VerticalContentAlignment="Center"
                              SelectionChanged="CboMonth_SelectionChanged" Cursor="Hand">
                    </ComboBox>
                </StackPanel>
            </Grid>

            <Border Grid.Row="1" Background="White" CornerRadius="16" Padding="40" Margin="0,0,0,20">
                <Border.Effect>
                    <DropShadowEffect Color="#000000" Opacity="0.05" BlurRadius="20" ShadowDepth="5"/>
                </Border.Effect>
                
                <StackPanel x:Name="pnlPayslipContent">
                    
                    <Grid Margin="0,0,0,30">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <StackPanel>
                            <TextBlock Text="{Binding HoTen}" FontSize="22" FontWeight="Bold" Foreground="#2563EB"/>
                            <TextBlock Text="{Binding TenChucVu}" FontSize="14" Foreground="#6B7280" Margin="0,4,0,0"/>
                            <TextBlock Text="{Binding TenPB}" FontSize="14" Foreground="#6B7280"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1" HorizontalAlignment="Right">
                            <TextBlock Text="KỲ TÍNH LƯƠNG" FontSize="12" FontWeight="Bold" Foreground="#9CA3AF" HorizontalAlignment="Right"/>
                            <TextBlock Text="{Binding TuNgay, StringFormat='dd/MM/yyyy'}" FontSize="16" FontWeight="SemiBold" HorizontalAlignment="Right" Margin="0,4,0,0"/>
                            <TextBlock Text="đến" FontSize="12" Foreground="#9CA3AF" HorizontalAlignment="Right"/>
                            <TextBlock Text="{Binding DenNgay, StringFormat='dd/MM/yyyy'}" FontSize="16" FontWeight="SemiBold" HorizontalAlignment="Right"/>
                        </StackPanel>
                    </Grid>

                    <Separator Background="#E5E7EB" Margin="0,0,0,30"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/> <ColumnDefinition Width="40"/> <ColumnDefinition Width="*"/> </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="KHOẢN THU NHẬP" Style="{StaticResource SectionHeader}"/>
                            
                            <Grid Margin="0,0,0,12">
                                <TextBlock Text="Lương Cơ Bản" Style="{StaticResource LabelText}"/>
                                <TextBlock Text="{Binding LuongCoBan, StringFormat={}{0:N0} đ}" Style="{StaticResource ValueText}"/>
                            </Grid>
                            
                            <Grid Margin="0,0,0,12">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="Lương Ngày Công" Style="{StaticResource LabelText}"/>
                                    <TextBlock Text="{Binding TongGioLam, StringFormat=' ({0} giờ)'}" FontSize="12" Foreground="#9CA3AF" VerticalAlignment="Center"/>
                                </StackPanel>
                                <TextBlock Text="{Binding LuongTheoNgayCong, StringFormat={}{0:N0} đ}" Style="{StaticResource ValueText}"/>
                            </Grid>

                            <Grid Margin="0,0,0,12">
                                <TextBlock Text="Tổng Phụ Cấp/Thưởng" Style="{StaticResource LabelText}"/>
                                <TextBlock Text="{Binding TongPhuCap, StringFormat={}{0:N0} đ}" Style="{StaticResource ValueText}"/>
                            </Grid>

                            <Grid x:Name="grdKiemNhiem" Margin="0,0,0,12">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="Lương Kiêm Nhiệm" Style="{StaticResource LabelText}"/>
                                    <TextBlock Text="{Binding HeSoKiemNhiem, StringFormat=' (HS: {0})'}" FontSize="12" Foreground="#2563EB" VerticalAlignment="Center"/>
                                </StackPanel>
                                <TextBlock Text="{Binding TienKiemNhiem, StringFormat={}{0:N0} đ}" Style="{StaticResource ValueText}" Foreground="#2563EB"/>
                            </Grid>
                        </StackPanel>

                        <StackPanel Grid.Column="2">
                            <TextBlock Text="KHOẢN KHẤU TRỪ" Style="{StaticResource SectionHeader}"/>

                            <Grid Margin="0,0,0,12">
                                <TextBlock Text="Bảo Hiểm (XH, YT, TN)" Style="{StaticResource LabelText}"/>
                                <TextBlock Text="{Binding TongBaoHiem, StringFormat={}-{0:N0} đ}" Style="{StaticResource ValueText}" Foreground="#EF4444"/>
                            </Grid>

                            <Grid Margin="0,0,0,12">
                                <TextBlock Text="Thuế TNCN" Style="{StaticResource LabelText}"/>
                                <TextBlock Text="{Binding TongThue, StringFormat={}-{0:N0} đ}" Style="{StaticResource ValueText}" Foreground="#EF4444"/>
                            </Grid>

                            <Grid Margin="0,0,0,12">
                                <TextBlock Text="Đã Tạm Ứng" Style="{StaticResource LabelText}"/>
                                <TextBlock Text="{Binding TienUng, StringFormat={}-{0:N0} đ}" Style="{StaticResource ValueText}" Foreground="#F59E0B"/>
                            </Grid>
                        </StackPanel>
                    </Grid>

                    <Separator Background="#E5E7EB" Margin="0,30,0,30"/>

                    <Border Background="#F0F9FF" CornerRadius="12" Padding="20">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <StackPanel>
                                <TextBlock Text="THỰC LÃNH (NET PAY)" FontSize="14" FontWeight="Bold" Foreground="#3B82F6"/>
                                <Border x:Name="badgeStatus" Background="#D1FAE5" CornerRadius="4" HorizontalAlignment="Left" Padding="8,2" Margin="0,5,0,0">
                                    <TextBlock Text="{Binding TrangThai}" FontSize="12" FontWeight="SemiBold" Foreground="#059669"/>
                                </Border>
                            </StackPanel>

                            <TextBlock Grid.Column="1" Text="{Binding ThucLanh, StringFormat={}{0:N0} VNĐ}" 
                                       FontSize="28" FontWeight="Bold" Foreground="#1E3A8A" VerticalAlignment="Center"/>
                        </Grid>
                    </Border>
                    <StackPanel>
                        <TextBlock x:Name="txtNoData" Text="Không có dữ liệu lương cho tháng này." 
                                   FontSize="16" Foreground="Gray" HorizontalAlignment="Center" 
                                   Visibility="Collapsed" Margin="20"/>
                    </StackPanel>
                </StackPanel>
                
            </Border>
        </Grid>
    </ScrollViewer>
</UserControl>