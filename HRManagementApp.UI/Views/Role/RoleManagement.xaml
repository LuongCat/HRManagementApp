<UserControl x:Class="HRManagementApp.UI.Views.RoleManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:converters="clr-namespace:HRManagementApp.Converters;assembly=HRManagementApp.Models"
             xmlns:constants="clr-namespace:HRManagementApp.Constants;assembly=HRManagementApp.Models"
             mc:Ignorable="d"
             d:DesignHeight="700"
             d:DesignWidth="1000">

    <UserControl.Resources>
        <converters:PermissionToBooleanConverter x:Key="PermToBool"/>
        <Style x:Key="ModernButtonStyle"
               TargetType="Button">
            <Setter Property="Padding"
                    Value="16,10"/>
            <Setter Property="FontSize"
                    Value="14"/>
            <Setter Property="FontWeight"
                    Value="SemiBold"/>
            <Setter Property="BorderThickness"
                    Value="0"/>
            <Setter Property="Cursor"
                    Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver"
                                     Value="True">
                                <Setter TargetName="border"
                                        Property="Opacity"
                                        Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsPressed"
                                     Value="True">
                                <Setter TargetName="border"
                                        Property="Opacity"
                                        Value="0.8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernDataGrid"
               TargetType="DataGrid">
            <Setter Property="Background"
                    Value="White"/>
            <Setter Property="BorderThickness"
                    Value="0"/>
            <Setter Property="RowHeight"
                    Value="50"/>
            <Setter Property="GridLinesVisibility"
                    Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush"
                    Value="#F1F5F9"/>
            <Setter Property="HeadersVisibility"
                    Value="Column"/>
            <Setter Property="AutoGenerateColumns"
                    Value="False"/>
            <Setter Property="CanUserAddRows"
                    Value="False"/>
            <Setter Property="CanUserResizeRows"
                    Value="False"/>
            <Setter Property="FontSize"
                    Value="14"/>

            <Setter Property="ColumnHeaderStyle">
                <Setter.Value>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background"
                                Value="Transparent"/>
                        <Setter Property="Foreground"
                                Value="#64748B"/>
                        <Setter Property="FontWeight"
                                Value="SemiBold"/>
                        <Setter Property="Padding"
                                Value="12"/>
                        <Setter Property="BorderThickness"
                                Value="0,0,0,1"/>
                        <Setter Property="BorderBrush"
                                Value="#E2E8F0"/>
                        <Setter Property="HorizontalContentAlignment"
                                Value="Left"/>
                    </Style>
                </Setter.Value>
            </Setter>

            <Setter Property="CellStyle">
                <Setter.Value>
                    <Style TargetType="DataGridCell">
                        <Setter Property="BorderThickness"
                                Value="0"/>
                        <Setter Property="Padding"
                                Value="12,0"/>
                        <Setter Property="VerticalContentAlignment"
                                Value="Center"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="DataGridCell">
                                    <Border Background="{TemplateBinding Background}"
                                            Padding="{TemplateBinding Padding}">
                                        <ContentPresenter VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Setter.Value>
            </Setter>

            <Setter Property="RowStyle">
                <Setter.Value>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Background"
                                Value="White"/>
                        <Setter Property="Cursor"
                                Value="Hand"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver"
                                     Value="True">
                                <Setter Property="Background"
                                        Value="#F8FAFC"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="#F1F5F9">
        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0"
                        Margin="0,0,0,24">
                <TextBlock Text="Quản lý Vai trò"
                           FontSize="28"
                           FontWeight="Bold"
                           Foreground="#1E293B"/>
                <TextBlock Text="Thiết lập và phân quyền truy cập cho nhân viên"
                           FontSize="14"
                           Foreground="#64748B"
                           Margin="0,4,0,0"/>
            </StackPanel>

            <Grid Grid.Row="1"
                  Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0"
                        Width="320"
                        Background="White"
                        CornerRadius="8"
                        BorderBrush="#E2E8F0"
                        BorderThickness="1">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="40"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Text="🔍"
                                   VerticalAlignment="Center"
                                   HorizontalAlignment="Center"
                                   Foreground="#94A3B8"/>

                        <TextBox x:Name="txtSearch"
                                 Grid.Column="1"
                                 Padding="0,10,10,10"
                                 BorderThickness="0"
                                 Background="Transparent"
                                 VerticalContentAlignment="Center"
                                 FontSize="14"
                                 TextChanged="TxtSearch_TextChanged"/>

                        <TextBlock Grid.Column="1"
                                   Text="Tìm kiếm vai trò..."
                                   Foreground="#94A3B8"
                                   VerticalAlignment="Center"
                                   FontSize="14"
                                   IsHitTestVisible="False">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility"
                                            Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Text.Length, ElementName=txtSearch}"
                                                     Value="0">
                                            <Setter Property="Visibility"
                                                    Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Grid>
                </Border>

                <StackPanel Grid.Column="2"
                            Orientation="Horizontal">
                    <Button Content="🔄 Làm mới"
                            Click="BtnReload_Click"
                            Style="{StaticResource ModernButtonStyle}"
                            Background="White"
                            Foreground="#64748B"
                            BorderBrush="#E2E8F0"
                            BorderThickness="1"
                            Margin="0,0,12,0"/>

                    <Button Content="+ Tạo mới vai trò"
                            Click="BtnAddRole_Click"
                            Style="{StaticResource ModernButtonStyle}"
                            Background="#1E293B"
                            Foreground="White"
                            IsEnabled="{Binding Converter={StaticResource PermToBool}, ConverterParameter={x:Static constants:AppPermissions.HETHONG_TAOVAITRO}}"/>
                </StackPanel>
            </Grid>

            <Border Grid.Row="2"
                    Background="White"
                    CornerRadius="12"
                    BorderBrush="#E2E8F0"
                    BorderThickness="1"
                    Padding="0,12,0,0">
                <Grid>
                    <DataGrid x:Name="dgRoles"
                              Style="{StaticResource ModernDataGrid}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Mã VT"
                                                Binding="{Binding MaVaiTro}"
                                                Width="80"/>

                            <DataGridTemplateColumn Header="Tên vai trò"
                                                    Width="200">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding TenVaiTro}"
                                                   FontWeight="Bold"
                                                   Foreground="#1E293B"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTextColumn Header="Mô tả"
                                                Binding="{Binding MoTa}"
                                                Width="*"/>

                            <DataGridTemplateColumn Header="Hành động"
                                    Width="260">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Content="✏️ Sửa"
                                                    Click="BtnEdit_Click"
                                                    CommandParameter="{Binding}"
                                                    FontSize="12"
                                                    FontWeight="Medium"
                                                    Padding="10,6"
                                                    Background="White"
                                                    Foreground="#F59E0B"
                                                    BorderBrush="#F59E0B"
                                                    BorderThickness="1"
                                                    Margin="0,0,8,0"
                                                    Cursor="Hand"
                                                    IsEnabled="{Binding Converter={StaticResource PermToBool}, ConverterParameter={x:Static constants:AppPermissions.HETHONG_SUAVAITRO}}">
                                                <Button.Resources>
                                                    <Style TargetType="Border">
                                                        <Setter Property="CornerRadius"
                                                                Value="6"/>
                                                    </Style>
                                                </Button.Resources>
                                            </Button>

                                            <Button Content="⚙ Phân quyền"
                                                    Click="BtnEditPermission_Click"
                                                    CommandParameter="{Binding}"
                                                    FontSize="12"
                                                    FontWeight="Medium"
                                                    Padding="10,6"
                                                    Background="#EFF6FF"
                                                    Foreground="#2563EB"
                                                    BorderThickness="0"
                                                    Margin="0,0,8,0"
                                                    Cursor="Hand"
                                                    IsEnabled="{Binding Converter={StaticResource PermToBool}, ConverterParameter={x:Static constants:AppPermissions.HETHONG_SUAVAITRO}}">
                                                <Button.Resources>
                                                    <Style TargetType="Border">
                                                        <Setter Property="CornerRadius"
                                                                Value="6"/>
                                                    </Style>
                                                </Button.Resources>
                                            </Button>

                                            <Button Content="🗑️ Xóa"
                                                    Click="BtnDelete_Click"
                                                    CommandParameter="{Binding}"
                                                    FontSize="12"
                                                    FontWeight="Medium"
                                                    Padding="10,6"
                                                    Background="#FEF2F2"
                                                    Foreground="#EF4444"
                                                    BorderThickness="0"
                                                    Cursor="Hand"
                                                    IsEnabled="{Binding Converter={StaticResource PermToBool}, ConverterParameter={x:Static constants:AppPermissions.HETHONG_XOAVAITRO}}">
                                                <Button.Resources>
                                                    <Style TargetType="Border">
                                                        <Setter Property="CornerRadius"
                                                                Value="6"/>
                                                    </Style>
                                                </Button.Resources>
                                            </Button>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>