<UserControl x:Class="HRManagementApp.UI.Views.Report.PayrollReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Background="White" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0 0 0 15">
            <TextBlock Text="Payroll Report" FontSize="22" FontWeight="Bold" Foreground="#2C3E50"/>
            <TextBlock Text="Lương, phụ cấp và khấu trừ của nhân viên"
                       FontSize="14" Foreground="#7F8C8D"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0 0 0 15">
            <ComboBox Width="200" Height="32">
                <ComboBoxItem Content="Select Month"/>
                <ComboBoxItem Content="January"/>
                <ComboBoxItem Content="February"/>
                <ComboBoxItem Content="March"/>
            </ComboBox>

            <Button Content="📄 Export PDF"
                    Width="150"
                    Height="32"
                    Background="#2E86AB"
                    Foreground="White"
                    BorderThickness="0"/>
        </StackPanel>

        <DataGrid Grid.Row="2"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  AlternatingRowBackground="#FAFAFA">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Employee" Binding="{Binding Name}" Width="200"/>
                <DataGridTextColumn Header="Base Salary" Binding="{Binding Salary}" Width="150"/>
                <DataGridTextColumn Header="Allowance" Binding="{Binding Allowance}" Width="150"/>
                <DataGridTextColumn Header="Deduction" Binding="{Binding Deduction}" Width="150"/>
                <DataGridTextColumn Header="Net Pay" Binding="{Binding NetPay}" Width="150"/>
            </DataGrid.Columns>

        </DataGrid>

    </Grid>
</UserControl>
