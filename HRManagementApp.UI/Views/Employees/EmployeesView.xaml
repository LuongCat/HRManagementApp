<UserControl x:Class="HRManagementApp.UI.Views.EmployeesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:HRManagementApp.Converters"
             xmlns:converters1="clr-namespace:HRManagementApp.Converters;assembly=HRManagementApp.Models"
             xmlns:constants="clr-namespace:HRManagementApp.Constants;assembly=HRManagementApp.Models"

             Background="#F5F7FA">

    <UserControl.Resources>
        <converters:StatusToColorConverter x:Key="StatusToColorConverter"/>
        <converters:StatusToBackgroundConverter x:Key="StatusToBackgroundConverter"/>
        <converters:TextToVisibilityConverter x:Key="TextToVisibilityConverter"/>
        <converters1:PermissionToBooleanConverter x:Key="PermToBool"/>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ✅ CONTENT CONTAINER -->
        <Border Grid.Row="0" Background="White" CornerRadius="12" Padding="25">
            <Border.Effect>
                <DropShadowEffect Color="#E0E0E0" BlurRadius="10" ShadowDepth="2" Opacity="0.3"/>
            </Border.Effect>
            
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Header với title và button -->
                <Grid Grid.Row="0" Margin="0,0,0,20">
                    <TextBlock Text="Danh sách nhân viên" 
                               FontSize="18" FontWeight="SemiBold" Foreground="#1F2937"
                               VerticalAlignment="Center" HorizontalAlignment="Left"/>

                    <Button x:Name="BtnThemNV" Content="+ Thêm nhân viên" 
                            Background="#1F2937" Foreground="White"
                            BorderThickness="0" Padding="20,10" FontSize="14" FontWeight="SemiBold"
                            Height="40" Cursor="Hand" HorizontalAlignment="Right"
                            Click="BtnThemNV_Click"
                            IsEnabled="{Binding Converter={StaticResource PermToBool}, ConverterParameter={x:Static constants:AppPermissions.NHANVIEN_THEM}}">                        
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" 
                                                    CornerRadius="8" Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#374151"/>
                                    </Trigger>
                                    
                                    <Trigger Property="IsEnabled" Value="False">
                                        <Setter Property="Opacity" Value="0.5"/> <Setter Property="Background" Value="#9CA3AF"/> <Setter Property="Cursor" Value="No"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>

                <!-- Search Box -->
                <Border Grid.Row="1" Background="#F9FAFB" CornerRadius="8" Padding="15,12" Margin="0,0,0,20">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <TextBlock Grid.Column="0" Text="🔍" FontSize="18" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        
                        <TextBox Grid.Column="1" x:Name="TxtSearch" 
                                 Background="Transparent" BorderThickness="0"
                                 FontSize="14" Foreground="#6B7280"
                                 VerticalContentAlignment="Center"
                                 TextChanged="TxtSearch_TextChanged">
                            <TextBox.Style>
                                <Style TargetType="TextBox">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="TextBox">
                                                <Grid>
                                                    <TextBlock Text="Tìm kiếm theo tên, mã NV, phòng ban..." 
                                                               Foreground="#9CA3AF" FontSize="14"
                                                               VerticalAlignment="Center"
                                                               Visibility="{Binding Text, ElementName=TxtSearch, Converter={StaticResource TextToVisibilityConverter}}"
                                                               GotKeyboardFocus="TextBox_GotKeyboardFocus" 
                                                               PreviewMouseLeftButtonDown="TextBox_PreviewMouseLeftButtonDown"/>
                                                    <ScrollViewer x:Name="PART_ContentHost" VerticalAlignment="Center"/>
                                                </Grid>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </TextBox.Style>
                        </TextBox>
                    </Grid>
                </Border>

                <!-- DataGrid -->
                <DataGrid Grid.Row="2" x:Name="EmployeesDataGrid" 
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          CanUserAddRows="False"
                          SelectionMode="Single"
                          RowHeight="70"
                          Margin="0,0,0,30"
                          FontSize="14"
                          GridLinesVisibility="None"
                          HeadersVisibility="Column"
                          BorderThickness="0"
                          Background="White">

                    <DataGrid.Resources>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Foreground" Value="#6B7280"/>
                            <Setter Property="FontSize" Value="13"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="Height" Value="45"/>
                            <Setter Property="Padding" Value="10,0"/>
                            <Setter Property="BorderThickness" Value="0,0,0,1"/>
                            <Setter Property="BorderBrush" Value="#E5E7EB"/>
                        </Style>
                        
                        <Style TargetType="DataGridRow">
                            <Setter Property="Background" Value="White"/>
                            <Setter Property="BorderThickness" Value="0,0,0,1"/>
                            <Setter Property="BorderBrush" Value="#F3F4F6"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#F9FAFB"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>

                        <Style TargetType="DataGridCell">
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Padding" Value="10,0"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="DataGridCell">
                                        <Border Background="{TemplateBinding Background}" 
                                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </DataGrid.Resources>

                    <DataGrid.Columns>
                        <!-- Mã NV -->
                        <DataGridTextColumn Header="Mã NV" Binding="{Binding MaNV}" Width="100">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                    <Setter Property="Foreground" Value="#1F2937"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <!-- Họ tên -->
                        <DataGridTextColumn Header="Họ tên" Binding="{Binding HoTen}" Width="180">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="#1F2937"/>
                                    <Setter Property="FontWeight" Value="Medium"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <!-- Ngày sinh -->
                        <DataGridTextColumn Header="Ngày sinh" Binding="{Binding NgaySinh, StringFormat=dd/MM/yyyy}" Width="110">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="#6B7280"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <!-- Điện thoại -->
                        <DataGridTextColumn Header="Điện thoại" Binding="{Binding DienThoai}" Width="120">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="#6B7280"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <!-- Phòng ban -->
                        <DataGridTextColumn Header="Phòng ban" Binding="{Binding PhongBanDisplay}" Width="140">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="#1F2937"/>
                                    <Setter Property="TextWrapping" Value="Wrap"/>
                                    <Setter Property="LineHeight" Value="18"/>
                                    <Setter Property="Padding" Value="0,4"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <!-- Chức vụ -->
                        <DataGridTextColumn Header="Chức vụ" Binding="{Binding ChucVuDisplay}" Width="140">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="#6B7280"/>
                                    <Setter Property="TextWrapping" Value="Wrap"/>
                                    <Setter Property="LineHeight" Value="18"/>
                                    <Setter Property="Padding" Value="0,4"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <!-- Trạng thái -->
                        <DataGridTemplateColumn Header="Trạng thái" Width="120">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border Background="{Binding TrangThai, Converter={StaticResource StatusToBackgroundConverter}}" 
                                            CornerRadius="12" Padding="12,4" 
                                            HorizontalAlignment="Center">
                                        <TextBlock Text="{Binding TrangThai}" 
                                                   Foreground="{Binding TrangThai, Converter={StaticResource StatusToColorConverter}}" 
                                                   FontSize="12" FontWeight="SemiBold"
                                                   HorizontalAlignment="Center"/>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- Thao tác -->
                        <DataGridTemplateColumn Header="Thao tác" Width="180">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <!-- Xem chi tiết -->
                                        <Button Content="👁" 
                                                Background="Transparent" 
                                                BorderThickness="0"
                                                FontSize="18"
                                                Width="35" Height="35"
                                                Cursor="Hand"
                                                Margin="3,0"
                                                ToolTip="Xem chi tiết"
                                                Tag="{Binding MaNV}"
                                                Click="BtnView_Click">
                                            <Button.Style>
                                                <Style TargetType="Button">
                                                    <Setter Property="Template">
                                                        <Setter.Value>
                                                            <ControlTemplate TargetType="Button">
                                                                <Border Background="{TemplateBinding Background}" 
                                                                        CornerRadius="6" 
                                                                        Width="{TemplateBinding Width}"
                                                                        Height="{TemplateBinding Height}">
                                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                </Border>
                                                            </ControlTemplate>
                                                        </Setter.Value>
                                                    </Setter>
                                                    <Style.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Background" Value="#E0E7FF"/>
                                                        </Trigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                        </Button>

                                        <!-- Chỉnh sửa -->
                                        <Button Content="✏️" 
                                                Background="Transparent" 
                                                BorderThickness="0"
                                                FontSize="16"
                                                Width="35" Height="35"
                                                Cursor="Hand"
                                                Margin="3,0"
                                                ToolTip="Chỉnh sửa"
                                                Tag="{Binding MaNV}"
                                                Click="BtnEdit_Click"
                                                IsEnabled="{Binding Converter={StaticResource PermToBool}, ConverterParameter={x:Static constants:AppPermissions.NHANVIEN_SUA}}">                                            <Button.Style>
                                                <Style TargetType="Button">
                                                    <Setter Property="Template">
                                                        <Setter.Value>
                                                            <ControlTemplate TargetType="Button">
                                                                <Border Background="{TemplateBinding Background}" 
                                                                        CornerRadius="6"
                                                                        Width="{TemplateBinding Width}"
                                                                        Height="{TemplateBinding Height}">
                                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                </Border>
                                                            </ControlTemplate>
                                                        </Setter.Value>
                                                    </Setter>
                                                    <Style.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Background" Value="#DBEAFE"/>
                                                        </Trigger>
                                                        <Trigger Property="IsEnabled" Value="False">
                                                            <Setter Property="Opacity" Value="0.3"/> 
                                                            <Setter Property="Cursor" Value="Arrow"/>
                                                        </Trigger>
                                                    </Style.Triggers>
                                                    
                                                </Style>
                                            </Button.Style>
                                        </Button>

                                        <!-- Chuyển trạng thái -->
                                        <Button Content="🔄" 
                                                Background="Transparent" 
                                                BorderThickness="0"
                                                FontSize="16"
                                                Width="35" Height="35"
                                                Cursor="Hand"
                                                Margin="3,0"
                                                ToolTip="Chuyển trạng thái"
                                                Tag="{Binding MaNV}"
                                                Click="BtnEditStatus_Click"
                                                IsEnabled="{Binding Converter={StaticResource PermToBool}, ConverterParameter={x:Static constants:AppPermissions.NHANVIEN_SUA}}">
                                            <Button.Style>
                                                <Style TargetType="Button">
                                                    <Setter Property="Template">
                                                        <Setter.Value>
                                                            <ControlTemplate TargetType="Button">
                                                                <Border Background="{TemplateBinding Background}" 
                                                                        CornerRadius="6"
                                                                        Width="{TemplateBinding Width}"
                                                                        Height="{TemplateBinding Height}">
                                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                </Border>
                                                            </ControlTemplate>
                                                        </Setter.Value>
                                                    </Setter>
                                                    <Style.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Background" Value="#FEF3C7"/>
                                                        </Trigger>
                                                        <Trigger Property="IsEnabled" Value="False">
                                                            <Setter Property="Opacity" Value="0.3"/> 
                                                            <Setter Property="Cursor" Value="Arrow"/>
                                                        </Trigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                        </Button>

                                        <!-- Xóa -->
                                        <Button Content="🗑️" 
                                                Background="Transparent" 
                                                BorderThickness="0"
                                                FontSize="16"
                                                Width="35" Height="35"
                                                Cursor="Hand"
                                                Margin="3,0"
                                                ToolTip="Xóa"
                                                Tag="{Binding MaNV}"
                                                Click="BtnDelete_Click"
                                                IsEnabled="{Binding Converter={StaticResource PermToBool}, ConverterParameter={x:Static constants:AppPermissions.NHANVIEN_XOA}}">                                            <Button.Style>
                                                <Style TargetType="Button">
                                                    <Setter Property="Template">
                                                        <Setter.Value>
                                                            <ControlTemplate TargetType="Button">
                                                                <Border Background="{TemplateBinding Background}" 
                                                                        CornerRadius="6"
                                                                        Width="{TemplateBinding Width}"
                                                                        Height="{TemplateBinding Height}">
                                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                </Border>
                                                            </ControlTemplate>
                                                        </Setter.Value>
                                                    </Setter>
                                                    <Style.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Background" Value="#FEE2E2"/>
                                                        </Trigger>
                                                        <Trigger Property="IsEnabled" Value="False">
                                                            <Setter Property="Opacity" Value="0.3"/> 
                                                            <Setter Property="Cursor" Value="Arrow"/>
                                                        </Trigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                        </Button>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>