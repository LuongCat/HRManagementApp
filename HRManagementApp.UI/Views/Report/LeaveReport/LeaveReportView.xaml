<UserControl x:Class="HRManagementApp.UI.Views.Report.LeaveReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Background="White" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0 0 0 15">
            <TextBlock Text="Leave Report" FontSize="22" FontWeight="Bold" Foreground="#2C3E50"/>
            <TextBlock Text="Nghỉ phép – ngày phép – lý do nghỉ"
                       FontSize="14" Foreground="#7F8C8D"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0 0 0 15">
            <ComboBox Width="180">
                <ComboBoxItem Content="All Leave Types"/>
                <ComboBoxItem Content="Annual Leave"/>
                <ComboBoxItem Content="Sick Leave"/>
                <ComboBoxItem Content="Unpaid Leave"/>
            </ComboBox>

            <Button Content="📄 Export"
                    Width="120"
                    Height="32"
                    Background="#2E86AB"
                    Foreground="White"/>
        </StackPanel>

        <DataGrid Grid.Row="2"
                  AutoGenerateColumns="False"
                  IsReadOnly="True">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Employee" Binding="{Binding Name}" Width="200"/>
                <DataGridTextColumn Header="Leave Type" Binding="{Binding Type}" Width="150"/>
                <DataGridTextColumn Header="From" Binding="{Binding From}" Width="150"/>
                <DataGridTextColumn Header="To" Binding="{Binding To}" Width="150"/>
                <DataGridTextColumn Header="Reason" Binding="{Binding Reason}" Width="200"/>
            </DataGrid.Columns>

        </DataGrid>
    </Grid>

</UserControl>
