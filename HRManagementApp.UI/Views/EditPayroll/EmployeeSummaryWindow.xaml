<Window x:Class="HRManagementApp.UI.Views.EmployeeSummaryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Tổng hợp Phiếu lương" Height="750" Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="#F3F4F6">

    <Window.Resources>
        <Style x:Key="SectionHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>

        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="0,0,0,20"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style x:Key="MoneyTextStyle" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
        </Style>
    </Window.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="25">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <RowDefinition Height="Auto"/> <RowDefinition Height="Auto"/> </Grid.RowDefinitions>

            <Border Grid.Row="0" Style="{StaticResource CardStyle}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Width="60" Height="60" CornerRadius="30" Background="#DBEAFE" Margin="0,0,20,0">
                        <TextBlock Text="NV" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold" Foreground="#2563EB" FontSize="20"/>
                    </Border>

                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock x:Name="TxtHoTen" Text="NGUYỄN VĂN A" FontSize="20" FontWeight="Bold" Foreground="#1F2937"/>
                        <TextBlock x:Name="TxtMaNV" Text="Mã NV: 001" FontSize="14" Foreground="#6B7280" Margin="0,5,0,0"/>
                    </StackPanel>
                    
                    <Border Grid.Column="2" Background="#EFF6FF" CornerRadius="8" Padding="15,5">
                        <StackPanel>
                            <TextBlock Text="Kỳ lương tháng" FontSize="12" Foreground="#6B7280"/>
                            <TextBlock x:Name="TxtThangHienTai" Text="11 / 2025" FontSize="18" FontWeight="Bold" Foreground="#2563EB" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/> <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="*"/> </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    
                    <Border Style="{StaticResource CardStyle}">
                        <StackPanel>
                            <TextBlock Text="💼 Lương cơ bản &amp; Chức vụ" Style="{StaticResource SectionHeaderStyle}" Foreground="#059669"/>
                            
                            <DataGrid x:Name="DgRoles" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column" BorderThickness="0" GridLinesVisibility="Horizontal" Background="White" RowHeight="40">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Chức vụ" Binding="{Binding ChucVu.TenCV}" Width="*"/>
                                    <DataGridTextColumn Header="HS Lương" Binding="{Binding HeSoLuongCoBan}" Width="80"/>
                                    <DataGridTextColumn Header="HS PC" Binding="{Binding HeSoPhuCapKiemNhiem}" Width="80"/>
                                </DataGrid.Columns>
                            </DataGrid>
                            
                            <Separator Margin="0,15"/>
                            
                            <DockPanel LastChildFill="False">
                                <TextBlock Text="Tổng lương theo hệ số:" Foreground="#4B5563"/>
                                <TextBlock x:Name="TxtTongLuongHeSo" Text="15,000,000 VNĐ" Style="{StaticResource MoneyTextStyle}" DockPanel.Dock="Right" Foreground="#059669"/>
                            </DockPanel>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource CardStyle}">
                        <StackPanel>
                            <TextBlock Text="💰 Các khoản phụ cấp" Style="{StaticResource SectionHeaderStyle}" Foreground="#059669"/>

                            <DataGrid x:Name="DgAllowances" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="None" BorderThickness="0" GridLinesVisibility="Horizontal" Background="White" RowHeight="35" MaxHeight="200">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Binding="{Binding TenPhuCap}" Width="*"/>
                                    <DataGridTextColumn Binding="{Binding SoTien, StringFormat={}{0:N0}}" Width="Auto" FontWeight="SemiBold"/>
                                </DataGrid.Columns>
                            </DataGrid>
                            
                            <Separator Margin="0,15"/>
                            
                            <DockPanel LastChildFill="False">
                                <TextBlock Text="Tổng phụ cấp:" Foreground="#4B5563"/>
                                <TextBlock x:Name="TxtTongPhuCap" Text="2,000,000 VNĐ" Style="{StaticResource MoneyTextStyle}" DockPanel.Dock="Right" Foreground="#059669"/>
                            </DockPanel>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <StackPanel Grid.Column="2">
                    
                    <Border Style="{StaticResource CardStyle}">
                        <StackPanel>
                            <TextBlock Text="📊 Thuế thu nhập" Style="{StaticResource SectionHeaderStyle}" Foreground="#D97706"/>

                            <DataGrid x:Name="DgTaxes" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="None" BorderThickness="0" GridLinesVisibility="Horizontal" Background="White" RowHeight="35" MaxHeight="150">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Binding="{Binding TenThue}" Width="*"/>
                                    <DataGridTextColumn Binding="{Binding SoTien, StringFormat={}{0:N0}}" Width="Auto" FontWeight="SemiBold" Foreground="#D97706"/>
                                </DataGrid.Columns>
                            </DataGrid>

                            <Separator Margin="0,15"/>

                            <DockPanel LastChildFill="False">
                                <TextBlock Text="Tổng thuế:" Foreground="#4B5563"/>
                                <TextBlock x:Name="TxtTongThue" Text="500,000 VNĐ" Style="{StaticResource MoneyTextStyle}" DockPanel.Dock="Right" Foreground="#D97706"/>
                            </DockPanel>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource CardStyle}">
                        <StackPanel>
                            <TextBlock Text="📉 Khấu trừ &amp; Phạt (Tháng này)" Style="{StaticResource SectionHeaderStyle}" Foreground="#DC2626"/>

                            <DataGrid x:Name="DgDeductions" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="None" BorderThickness="0" GridLinesVisibility="Horizontal" Background="White" RowHeight="35" MaxHeight="150">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Binding="{Binding TenKhoanTru}" Width="*"/>
                                    <DataGridTextColumn Binding="{Binding SoTien, StringFormat={}{0:N0}}" Width="Auto" FontWeight="SemiBold" Foreground="#DC2626"/>
                                </DataGrid.Columns>
                            </DataGrid>

                            <Separator Margin="0,15"/>

                            <DockPanel LastChildFill="False">
                                <TextBlock Text="Tổng phạt:" Foreground="#4B5563"/>
                                <TextBlock x:Name="TxtTongPhat" Text="200,000 VNĐ" Style="{StaticResource MoneyTextStyle}" DockPanel.Dock="Right" Foreground="#DC2626"/>
                            </DockPanel>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>

            <Border Grid.Row="2" Background="#2563EB" CornerRadius="12" Padding="25">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="TỔNG THỰC NHẬN" FontSize="14" Foreground="#BFDBFE" FontWeight="SemiBold"/>
                        <TextBlock Text="(Thu nhập - Các khoản giảm trừ)" FontSize="12" Foreground="#93C5FD" FontStyle="Italic"/>
                    </StackPanel>

                    <TextBlock x:Name="TxtThucNhan" Grid.Column="1" 
                               Text="16,300,000 VNĐ" 
                               FontSize="32" 
                               FontWeight="Bold" 
                               Foreground="White" 
                               VerticalAlignment="Center"/>
                </Grid>
            </Border>

        </Grid>
    </ScrollViewer>
</Window>