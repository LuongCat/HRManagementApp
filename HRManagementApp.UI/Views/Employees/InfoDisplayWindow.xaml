<Window x:Class="HRManagementApp.UI.Views.InfoDisplayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Thông tin chi tiết" 
        Height="600" Width="800"
        WindowStartupLocation="CenterScreen"
        Background="#F5F7FA"
        AllowsTransparency="False"

        WindowStyle="None"
        ResizeMode="NoResize">

    <Border Background="#F5F7FA" CornerRadius="16">
        

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- ✅ HEADER -->
            <Border Grid.Row="0" Background="#1F2937" CornerRadius="16,16,0,0" Padding="30,20">
                <Grid>
                    <StackPanel>
                        <TextBlock x:Name="TxtTitle" Text="THÔNG TIN CHI TIẾT" 
                                   FontSize="20" FontWeight="Bold" Foreground="White"/>
                        <TextBlock x:Name="TxtSubtitle" Text="" 
                                   FontSize="16" Foreground="#D1D5DB" Margin="0,5,0,0"/>
                    </StackPanel>

                    <!-- Close Button -->
                    <Button Content="✕" 
                            HorizontalAlignment="Right" VerticalAlignment="Top"
                            Width="35" Height="35"
                            Background="Transparent" 
                            Foreground="White"
                            BorderThickness="0"
                            FontSize="20"
                            Cursor="Hand"
                            Click="BtnClose_Click">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" 
                                                    CornerRadius="6"
                                                    Width="{TemplateBinding Width}"
                                                    Height="{TemplateBinding Height}">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#374151"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </Border>

            <!-- ✅ CONTENT (ScrollViewer cho nội dung dài) -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="30,25">
                <Border Background="White" CornerRadius="12" Padding="30">
                   
                    <StackPanel>
                    <!-- ItemsControl để hiển thị danh sách thông tin -->
                    <ItemsControl x:Name="InfoItemsControl">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,0,0,20">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="200"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Label -->
                                    <TextBlock Grid.Column="0" 
                                               Text="{Binding Label}" 
                                               FontSize="14" 
                                               Foreground="#6B7280"
                                               VerticalAlignment="Top"
                                               Margin="0,0,20,0"/>

                                    <!-- Value -->
                                    <TextBlock Grid.Column="1" 
                                               Text="{Binding Value}" 
                                               FontSize="15" 
                                               Foreground="{Binding ValueColor}"
                                               FontWeight="{Binding ValueWeight}"
                                               TextWrapping="Wrap"
                                               VerticalAlignment="Top"/>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    
                    <StackPanel x:Name="BarcodeSection" Margin="0,20,0,0" Visibility="Collapsed">
                            <Separator Background="#E5E7EB" Margin="0,0,0,20"/>
                            
                            <TextBlock Text="Mã Thẻ Nhân Viên (Barcode)" FontSize="14" FontWeight="Bold" Foreground="#374151" Margin="0,0,0,15"/>
                            
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <!-- Ảnh Barcode -->
                                <Border Grid.Column="0" BorderBrush="#E5E7EB" BorderThickness="1" CornerRadius="8" Padding="10">
                                    <Image x:Name="ImgBarcode" Height="80" Width="250" Stretch="Uniform"/>
                                </Border>

                                <!-- Nút tải -->
                                <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="20,0,0,0">
                                    <TextBlock x:Name="TxtBarcodeValue" Text="123456" FontSize="18" FontWeight="Bold" Foreground="#111827"/>
                                    <TextBlock Text="Sử dụng để chấm công" FontSize="12" Foreground="#6B7280" Margin="0,2,0,10"/>
                                    
                                    <Button Content="⬇ Tải mã vạch" 
                                            HorizontalAlignment="Left"
                                            Background="White" Foreground="#2563EB"
                                            BorderBrush="#2563EB" BorderThickness="1"
                                            Padding="15,8" FontSize="13" FontWeight="SemiBold"
                                            Cursor="Hand"
                                            Click="BtnDownloadBarcode_Click">
                                        <Button.Resources>
                                            <Style TargetType="Border">
                                                <Setter Property="CornerRadius" Value="6"/>
                                            </Style>
                                        </Button.Resources>
                                    </Button>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </ScrollViewer>

            <!-- ✅ FOOTER -->
            <Border Grid.Row="2" BorderBrush="#E5E7EB" BorderThickness="0,1,0,0" 
                    Background="#1F2937" CornerRadius="0,0,16,16" Padding="30,20">
                <Button Content="Đóng" 
                        Background="Black" Foreground="White"
                        BorderThickness="0"
                        Padding="25,12" FontSize="14" FontWeight="SemiBold"
                        Cursor="Hand"
                        HorizontalAlignment="Center"
                        Click="BtnClose_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" 
                                                CornerRadius="8" 
                                                Padding="{TemplateBinding Padding}"
                                                MinWidth="150">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#374151"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </Border>
        </Grid>
    </Border>
</Window>