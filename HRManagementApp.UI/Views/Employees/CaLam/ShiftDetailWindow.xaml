<Window x:Class="HRManagementApp.UI.Views.ShiftDetailWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Thông Tin Ca Làm Việc" Height="400" Width="500"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="White">

    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="White" CornerRadius="6" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#374151"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <RowDefinition Height="*"/>    <RowDefinition Height="Auto"/> </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#F9FAFB" Padding="20" BorderBrush="#E5E7EB" BorderThickness="0,0,0,1">
            <TextBlock Text="Cập Nhật Ca Làm" FontSize="18" FontWeight="Bold" Foreground="#111827"/>
        </Border>

        <StackPanel Grid.Row="1" Margin="30,20">
            
            <TextBlock Text="Tên Ca Làm"/>
            <TextBox x:Name="TxtTenCa" Tag="Nhập tên ca (VD: Ca Sáng)"/>

            <Grid Margin="0,20,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="15"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Giờ Bắt Đầu"/>
                    <TextBox x:Name="TxtGioBatDau" ToolTip="Định dạng HH:mm"/>
                </StackPanel>

                <StackPanel Grid.Column="2">
                    <TextBlock Text="Giờ Kết Thúc"/>
                    <TextBox x:Name="TxtGioKetThuc" ToolTip="Định dạng HH:mm"/>
                </StackPanel>
            </Grid>

            <TextBlock Text="Trạng Thái" Margin="0,20,0,8"/>
            <Border BorderBrush="#D1D5DB" BorderThickness="1" CornerRadius="6" Background="White">
                <ComboBox x:Name="CboTrangThai" Height="40" Padding="10,0" VerticalContentAlignment="Center" BorderThickness="0" Background="Transparent">
                    <ComboBoxItem Content="Hoạt động" IsSelected="True"/>
                    <ComboBoxItem Content="Tạm ngừng"/>
                </ComboBox>
            </Border>

        </StackPanel>

        <Border Grid.Row="2" Padding="20" Background="#F9FAFB" BorderBrush="#E5E7EB" BorderThickness="0,1,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                
                <Button Content="Hủy Bỏ" Click="BtnCancel_Click" 
                        Background="White" Foreground="#374151" 
                        Width="100" Height="40" BorderBrush="#D1D5DB" BorderThickness="1" Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border"> <Setter Property="CornerRadius" Value="6"/> </Style>
                    </Button.Resources>
                </Button>

                <Button Content="Lưu Thông Tin" Click="BtnSave_Click" 
                        Background="#2563EB" Foreground="White" FontWeight="Bold"
                        Width="140" Height="40" Margin="10,0,0,0" BorderThickness="0" Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border"> <Setter Property="CornerRadius" Value="6"/> </Style>
                    </Button.Resources>
                </Button>

            </StackPanel>
        </Border>
    </Grid>
</Window>