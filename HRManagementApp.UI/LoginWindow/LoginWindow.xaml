<Window x:Class="HRManagementApp.UI.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login"
        Height="550" Width="850"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        WindowStyle="None"
        Background="Transparent"
        MouseDown="Window_MouseDown">

    <Window.Resources>
        <Style x:Key="InputTextBox" TargetType="Control">
            <Setter Property="Height" Value="45"/>
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="#F8FAFC"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Control">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="White" CornerRadius="20" Margin="20">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="25" ShadowDepth="0" Opacity="0.15"/>
        </Border.Effect>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.1*"/> <ColumnDefinition Width="1.4*"/> </Grid.ColumnDefinitions>

            <Border Grid.Column="0" CornerRadius="20,0,0,20">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#2C3E50" Offset="0"/>
                        <GradientStop Color="#1ABC9C" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>

                <StackPanel Margin="40" VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,30">
                        <TextBlock Text="✨" FontSize="24" Margin="0,0,10,0"/>
                        <StackPanel>
                            <TextBlock Text="Quản Lý Nhân Sự" FontSize="18" FontWeight="Bold" Foreground="White"/>
                            <TextBlock Text="Hệ thống tích hợp chuyên nghiệp" FontSize="12" Foreground="#E0E7FF"/>
                        </StackPanel>
                    </StackPanel>

                    <TextBlock Text="Hãy Đến Với Chúng Tôi!" FontSize="18" FontWeight="Bold" Foreground="White" Margin="0,0,0,10"/>
                    <TextBlock Text="Nơi mọi người được trân trọng, phát triển và thành công cùng nhau." 
                               Foreground="#E0E7FF" FontSize="14" TextWrapping="Wrap" Margin="0,0,0,30"/>

                    <StackPanel Margin="0,0,0,20">
                        <Grid Margin="0,0,0,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Border Width="32" Height="32" CornerRadius="16" Background="#30FFFFFF" VerticalAlignment="Top">
                                <TextBlock Text="✓" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel Grid.Column="1" Margin="15,0,0,0">
                                <TextBlock Text="Quản lý hiệu quả" FontWeight="Bold" Foreground="White"/>
                                <TextBlock Text="Theo dõi chuyên cần, nghỉ phép và hiệu suất làm việc dễ dàng." Foreground="#E0E7FF" FontSize="12" TextWrapping="Wrap" Margin="0,2,0,0"/>
                            </StackPanel>
                        </Grid>

                        <Grid Margin="0,0,0,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Border Width="32" Height="32" CornerRadius="16" Background="#30FFFFFF" VerticalAlignment="Top">
                                <TextBlock Text="🛡" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel Grid.Column="1" Margin="15,0,0,0">
                                <TextBlock Text="Bảo mật tuyệt đối" FontWeight="Bold" Foreground="White"/>
                                <TextBlock Text="Dữ liệu được mã hóa và bảo vệ với công nghệ tiên tiến." Foreground="#E0E7FF" FontSize="12" TextWrapping="Wrap" Margin="0,2,0,0"/>
                            </StackPanel>
                        </Grid>
                        
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Border Width="32" Height="32" CornerRadius="16" Background="#30FFFFFF" VerticalAlignment="Top">
                                <TextBlock Text="⚡" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel Grid.Column="1" Margin="15,0,0,0">
                                <TextBlock Text="Trải nghiệm mượt mà" FontWeight="Bold" Foreground="White"/>
                                <TextBlock Text="Giao diện thân thiện, dễ sử dụng cho mọi đối tượng." Foreground="#E0E7FF" FontSize="12" TextWrapping="Wrap" Margin="0,2,0,0"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </StackPanel>
            </Border>

            <Grid Grid.Column="1">
                <Button Content="✕" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="20"
                        Background="Transparent" BorderThickness="0" Foreground="#94A3B8" FontSize="16" 
                        Cursor="Hand" Click="BtnClose_Click"/>
                <TabControl Margin="40,30,40,20" BorderThickness="0" Background="Transparent">
                    <TabControl.Resources>
                        <Style TargetType="TabItem">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="TabItem">
                                        <Grid x:Name="Panel">
                                            <ContentPresenter x:Name="ContentSite"
                                                              VerticalAlignment="Center"
                                                              HorizontalAlignment="Center"
                                                              ContentSource="Header"
                                                              
                                                              Margin="10,2,20,10"/>
                                            <Border x:Name="Underline" Background="#1ABC9C" Height="4" VerticalAlignment="Bottom" Visibility="Hidden"/>
                                        </Grid>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="Underline" Property="Visibility" Value="Visible"/>
                                                <Setter Property="FontWeight" Value="Bold"/>
                                                <Setter Property="Foreground" Value="#1ABC9C"/>
                                            </Trigger>
                                            <Trigger Property="IsSelected" Value="False">
                                                <Setter Property="Foreground" Value="#64748B" />
                                                <Setter Property="FontWeight" Value="Bold"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Setter Property="FontSize" Value="16"/>
                            <Setter Property="Cursor" Value="Hand"/>
                            <Setter Property="Margin" Value="0,0,20,10"/>
                        </Style>
                    </TabControl.Resources>
                    <TabItem Header="Đăng nhập">
                <StackPanel VerticalAlignment="Center" Margin="50,0">
                    <TextBlock Text="Chào Mừng Trở Lại!" FontSize="24" FontWeight="Bold" Foreground="#1E293B" Margin="0,0,0,8"/>
                    <TextBlock Text="Đăng nhập để tiếp tục sử dụng hệ thống" FontSize="14" Foreground="#64748B" Margin="0,0,0,30"/>

                    <TextBlock Text="Tên đăng nhập" Foreground="#475569" FontWeight="Medium" Margin="0,0,0,6"/>
                    <TextBox x:Name="UsernameTextBox" Style="{StaticResource InputTextBox}" Margin="0,0,0,20" />

                    <TextBlock Text="Mật khẩu" Foreground="#475569" FontWeight="Medium" Margin="0,0,0,6"/>
                    <Grid Margin="0,0,0,30">
                        <PasswordBox x:Name="PasswordBox" 
                                     Height="45" Padding="15,0" FontSize="14"
                                     BorderBrush="#E2E8F0" BorderThickness="1" Background="#F8FAFC"
                                     VerticalContentAlignment="Center">
                            <PasswordBox.Template>
                                <ControlTemplate TargetType="PasswordBox">
                                    <Border Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            CornerRadius="8">
                                        <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </PasswordBox.Template>
                        </PasswordBox>
                    </Grid>

                    <TextBlock x:Name="ErrorText" Foreground="#DC2626" FontSize="13" 
                               HorizontalAlignment="Center" Margin="0,0,0,15" Visibility="Collapsed"/>

                    <Button Content="Đăng nhập ➜" Click="LoginButton_Click" IsDefault="True"
                            FontSize="16" FontWeight="Bold" Foreground="White" Height="50"
                            BorderThickness="0" Cursor="Hand">
                        <Button.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                <GradientStop Color="#1ABC9C" Offset="0"/>
                                <GradientStop Color="#16A085" Offset="1"/>
                            </LinearGradientBrush>
                        </Button.Background>
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="10">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Opacity" Value="0.9"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>

                </StackPanel>
                    </TabItem>
                        <TabItem Header="Đăng nhâp bằng Mã QR">
                            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,20">
                                <TextBlock Text="Quét mã để đăng nhập" FontSize="18" FontWeight="Bold" Foreground="#1E293B" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                                <TextBlock Text="Sử dụng ứng dụng mobile để quét mã bên dưới" FontSize="13" Foreground="#64748B" HorizontalAlignment="Center" Margin="0,0,0,30"/>

                                <Border Width="220" Height="220" BorderBrush="#E2E8F0" BorderThickness="2" CornerRadius="10" Background="#F8FAFC" Padding="10">
                                    <Image x:Name="QrImage" Stretch="Uniform"/>
                                </Border>

                                <Button x:Name="ShowQrBtn" Content="🔄 Lấy mã QR mới" Click="ShowQr_Click"
                                        Margin="0,30,0,0" Padding="20,10"
                                        Background="White" BorderBrush="#1ABC9C" BorderThickness="1"
                                        Foreground="#1ABC9C" FontWeight="SemiBold" Cursor="Hand">
                                    <Button.Resources>
                                        <Style TargetType="Border">
                                            <Setter Property="CornerRadius" Value="8"/>
                                        </Style>
                                    </Button.Resources>
                                </Button>
                            </StackPanel>
                        </TabItem>

                </TabControl>
            </Grid>
        </Grid>
    </Border>
</Window>