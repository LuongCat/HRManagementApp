<UserControl x:Class="HRManagementApp.UI.Views.Report.PayrollReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             mc:Ignorable="d">

        <UserControl.Resources>
                <Style x:Key="FilterComboBox"
                       TargetType="ComboBox">
                        <Setter Property="Padding"
                                Value="10,6"/>
                        <Setter Property="FontSize"
                                Value="14"/>
                        <Setter Property="Background"
                                Value="White"/>
                        <Setter Property="BorderBrush"
                                Value="#E2E8F0"/>
                        <Setter Property="BorderThickness"
                                Value="1"/>
                        <Setter Property="VerticalContentAlignment"
                                Value="Center"/>
                </Style>
        </UserControl.Resources>

        <Grid Margin="24">
                <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0"
                            Margin="0,0,0,24">
                        <TextBlock Text="Bảng Lương Nhân Viên"
                                   FontSize="24"
                                   FontWeight="Bold"
                                   Foreground="#1E293B"/>
                        <TextBlock Text="Xem chi tiết và xuất báo cáo lương hàng tháng"
                                   FontSize="14"
                                   Foreground="#64748B"
                                   Margin="0,4,0,0"/>
                </StackPanel>

                <!-- SUMMARY CARDS -->
                <UniformGrid Grid.Row="1"
                             Columns="4"
                             Margin="0,0,0,20">

                        <Border Background="#EEF2FF"
                                Padding="20"
                                CornerRadius="10"
                                Margin="10">
                                <StackPanel>
                                        <TextBlock Text="Tổng Lương"
                                                   FontWeight="Bold"
                                                   FontSize="16"/>
                                        <TextBlock x:Name="txtMonthlyPayroll"
                                                   FontSize="22"
                                                   FontWeight="Bold"
                                                   Foreground="#4F46E5"/>
                                </StackPanel>
                        </Border>

                        <Border Background="#F0FDFA"
                                Padding="20"
                                CornerRadius="10"
                                Margin="10">
                                <StackPanel>
                                        <TextBlock Text="Lương Trung Bình"
                                                   FontWeight="Bold"
                                                   FontSize="16"/>
                                        <TextBlock x:Name="txtAvgPayroll"
                                                   FontSize="22"
                                                   FontWeight="Bold"
                                                   Foreground="#0D9488"/>
                                </StackPanel>
                        </Border>

                        <Border Background="#FEF9C3"
                                Padding="20"
                                CornerRadius="10"
                                Margin="10">
                                <StackPanel>
                                        <TextBlock Text="Biến Động Tháng"
                                                   FontWeight="Bold"
                                                   FontSize="16"/>
                                        <TextBlock x:Name="txtPayrollChange"
                                                   FontSize="22"
                                                   FontWeight="Bold"
                                                   Foreground="#CA8A04"/>
                                </StackPanel>
                        </Border>

                        <Border Background="#FFE4E6"
                                Padding="20"
                                CornerRadius="10"
                                Margin="10">
                                <StackPanel>
                                        <TextBlock Text="Số NV Nhận Lương"
                                                   FontWeight="Bold"
                                                   FontSize="16"/>
                                        <TextBlock x:Name="txtPayrollEmployees"
                                                   FontSize="22"
                                                   FontWeight="Bold"
                                                   Foreground="#E11D48"/>
                                </StackPanel>
                        </Border>

                </UniformGrid>

                <Grid Grid.Row="2"
                      Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="300"/>
                        </Grid.ColumnDefinitions>

                        <lvc:CartesianChart x:Name="chartSalaryDept"
                                            Grid.Column="0"
                                            Height="200"/>

                        <lvc:PieChart x:Name="chartPayrollDonut"
                                      Grid.Column="1"
                                      Height="200"
                                      LegendPosition="Right"
                                      Margin="0,0,0,20"
                                      MaxAngle="360"/>
                </Grid>

                <lvc:CartesianChart x:Name="chartSalaryTrend"
                                    Grid.Row="3"
                                    Height="360"
                                    HorizontalAlignment="Stretch"
                                    VerticalAlignment="Stretch"/>

                <!-- PAYROLL TABLE SECTION -->
                <Grid Background="White"
                      Margin="24"
                      Grid.Row="4">
                        <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <Grid Grid.Row="0"
                              Margin="0,0,0,20">
                                <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0"
                                            Orientation="Horizontal">
                                        <Border Background="#F8FAFC"
                                                BorderBrush="#E2E8F0"
                                                BorderThickness="1"
                                                CornerRadius="8"
                                                Padding="4">
                                                <StackPanel Orientation="Horizontal">
                                                        <ComboBox x:Name="cboMonth"
                                                                  Width="100"
                                                                  Margin="0,0,8,0"
                                                                  Style="{StaticResource FilterComboBox}"
                                                                  SelectionChanged="Filter_SelectionChanged"/>

                                                        <ComboBox x:Name="cboYear"
                                                                  Width="100"
                                                                  Style="{StaticResource FilterComboBox}"
                                                                  SelectionChanged="Filter_SelectionChanged"/>
                                                </StackPanel>
                                        </Border>
                                </StackPanel>

                                <StackPanel Grid.Column="2"
                                            Orientation="Horizontal">
                                        <Button x:Name="btnExport"
                                                Content="📥 Xuất Excel"
                                                Click="BtnExport_Click"
                                                Background="#10B981"
                                                Foreground="White"
                                                FontWeight="SemiBold"
                                                FontSize="14"
                                                Padding="16,8"
                                                BorderThickness="0"
                                                Cursor="Hand">
                                                <Button.Resources>
                                                        <Style TargetType="Border">
                                                                <Setter Property="CornerRadius"
                                                                        Value="6"/>
                                                        </Style>
                                                </Button.Resources>
                                        </Button>
                                </StackPanel>
                        </Grid>

                        <DataGrid x:Name="dgPayroll"
                                  Grid.Row="2"
                                  MinHeight="500"
                                  AutoGenerateColumns="False"
                                  IsReadOnly="True"
                                  HeadersVisibility="Column"
                                  GridLinesVisibility="Horizontal"
                                  Background="White"
                                  BorderThickness="1"
                                  BorderBrush="#E2E8F0"
                                  RowHeight="48"
                                  HorizontalAlignment="Stretch"
                                  EnableRowVirtualization="True"
                                  EnableColumnVirtualization="True"
                                  AlternatingRowBackground="#F8FAFC">

                                <DataGrid.Resources>
                                        <Style TargetType="DataGridColumnHeader">
                                                <Setter Property="Background"
                                                        Value="Transparent"/>
                                                <Setter Property="Foreground"
                                                        Value="#64748B"/>
                                                <Setter Property="FontWeight"
                                                        Value="SemiBold"/>
                                                <Setter Property="Padding"
                                                        Value="12,10"/>
                                                <Setter Property="BorderThickness"
                                                        Value="0,0,0,1"/>
                                                <Setter Property="BorderBrush"
                                                        Value="#E2E8F0"/>
                                        </Style>
                                        <Style TargetType="DataGridCell">
                                                <Setter Property="Padding"
                                                        Value="12,0"/>
                                                <Setter Property="Template">
                                                        <Setter.Value>
                                                                <ControlTemplate TargetType="DataGridCell">
                                                                        <Border Padding="{TemplateBinding Padding}"
                                                                                Background="{TemplateBinding Background}">
                                                                                <ContentPresenter VerticalAlignment="Center"/>
                                                                        </Border>
                                                                </ControlTemplate>
                                                        </Setter.Value>
                                                </Setter>
                                                <Setter Property="BorderThickness"
                                                        Value="0"/>
                                        </Style>
                                </DataGrid.Resources>

                                <DataGrid.Columns>
                                        <DataGridTextColumn Header="Mã NV"
                                                            Binding="{Binding MaNV}"
                                                            Width="80"/>
                                        <DataGridTextColumn Header="Họ Tên"
                                                            Binding="{Binding EmployeeName}"
                                                            Width="1.5*"/>
                                        <DataGridTextColumn Header="Phòng Ban"
                                                            Binding="{Binding Department}"
                                                            Width="*"/>
                                        <DataGridTextColumn Header="Công"
                                                            Binding="{Binding WorkDays}"
                                                            Width="60"/>

                                        <DataGridTextColumn Header="Lương CB"
                                                            Binding="{Binding BaseSalary, StringFormat={}{0:N0}}"
                                                            Width="*"/>
                                        <DataGridTextColumn Header="Phụ Cấp"
                                                            Binding="{Binding TotalAllowance, StringFormat={}{0:N0}}"
                                                            Width="*"/>
                                        <DataGridTextColumn Header="Khấu Trừ"
                                                            Binding="{Binding TotalDeduction, StringFormat={}{0:N0}}"
                                                            Width="*"/>

                                        <DataGridTextColumn Header="Thực Lãnh"
                                                            Binding="{Binding NetPay, StringFormat={}{0:N0} đ}"
                                                            Width="*"
                                                            FontWeight="Bold"
                                                            Foreground="#2563EB"/>

                                        <DataGridTemplateColumn Header="Trạng Thái"
                                                                Width="100">
                                                <DataGridTemplateColumn.CellTemplate>
                                                        <DataTemplate>
                                                                <Border Background="{Binding StatusColor}"
                                                                        CornerRadius="4"
                                                                        Padding="8,4"
                                                                        HorizontalAlignment="Left">
                                                                        <TextBlock Text="{Binding Status}"
                                                                                   Foreground="White"
                                                                                   FontSize="11"
                                                                                   FontWeight="SemiBold"/>
                                                                </Border>
                                                        </DataTemplate>
                                                </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>
                                </DataGrid.Columns>
                        </DataGrid>
                </Grid>
        </Grid>
</UserControl>