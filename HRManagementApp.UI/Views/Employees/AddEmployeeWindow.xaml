<Window x:Class="HRManagementApp.UI.Views.AddEmployeeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Thêm nhân viên mới" 
        Height="750" Width="900"
        WindowStartupLocation="CenterScreen"
        Background="Transparent"
        AllowsTransparency="True"
        WindowStyle="None"
        ResizeMode="NoResize">

    <Border Background="#F5F7FA" CornerRadius="16" Margin="10">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- ✅ HEADER -->
            <Border Grid.Row="0" Background="#1F2937" CornerRadius="16,16,0,0" Padding="30,20">
                <Grid>
                    <StackPanel>
                        <TextBlock Text="THÊM NHÂN VIÊN MỚI" 
                                   FontSize="20" FontWeight="Bold" Foreground="White"/>
                        <TextBlock Text="Điền đầy đủ thông tin nhân viên" 
                                   FontSize="14" Foreground="#D1D5DB" Margin="0,5,0,0"/>
                    </StackPanel>

                    <!-- Close Button -->
                    <Button Content="✕" 
                            HorizontalAlignment="Right" VerticalAlignment="Top"
                            Width="35" Height="35"
                            Background="Transparent" 
                            Foreground="White"
                            BorderThickness="0"
                            FontSize="20"
                            Cursor="Hand"
                            Click="BtnClose_Click">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" 
                                                    CornerRadius="6"
                                                    Width="{TemplateBinding Width}"
                                                    Height="{TemplateBinding Height}">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#374151"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </Border>

            <!-- ✅ CONTENT -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="30,25">
                <StackPanel>
                    <!-- THÔNG TIN CƠ BẢN -->
                    <Border Background="White" CornerRadius="12" Padding="25" Margin="0,0,0,20">

                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="15"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- Section Title -->
                            <TextBlock Grid.Row="0" Text="Thông tin cơ bản" 
                                       FontSize="16" FontWeight="SemiBold" Foreground="#1F2937"/>

                            <!-- Form Grid -->
                            <Grid Grid.Row="2">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="20"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="15"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="15"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="15"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="15"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <!-- Row 1: Họ và tên -->
                                <StackPanel Grid.Row="0" Grid.Column="0">
                                    <TextBlock Text="Họ và tên *" FontSize="13" Foreground="#374151" FontWeight="SemiBold" Margin="0,0,0,8"/>
                                    <TextBox x:Name="TxtHoTen" 
                                             Height="40" Padding="12,0"
                                             FontSize="14" 
                                             BorderBrush="#D1D5DB" BorderThickness="1">
                                        <TextBox.Style>
                                            <Style TargetType="TextBox">
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="TextBox">
                                                            <Border Background="White" 
                                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                                    CornerRadius="8">
                                                                <ScrollViewer x:Name="PART_ContentHost" 
                                                                            Margin="{TemplateBinding Padding}"
                                                                            VerticalAlignment="Center"/>
                                                            </Border>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                                <Style.Triggers>
                                                    <Trigger Property="IsFocused" Value="True">
                                                        <Setter Property="BorderBrush" Value="#2563EB"/>
                                                        <Setter Property="BorderThickness" Value="2"/>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBox.Style>
                                    </TextBox>
                                </StackPanel>

                                <StackPanel Grid.Row="0" Grid.Column="2">
                                    <TextBlock Text="Ngày sinh *" FontSize="13" Foreground="#374151" FontWeight="SemiBold" Margin="0,0,0,8"/>
                                    <DatePicker x:Name="DpNgaySinh" 
                                                Height="40" 
                                                FontSize="14"
                                                BorderBrush="#D1D5DB" BorderThickness="1">
                                        <DatePicker.Resources>
                                            <Style TargetType="DatePickerTextBox">
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="DatePickerTextBox">
                                                            <Border Background="White" 
                                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                                    CornerRadius="8">
                                                                <ScrollViewer x:Name="PART_ContentHost" 
                                                                            Margin="12,0"
                                                                            VerticalAlignment="Center"/>
                                                            </Border>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style>
                                        </DatePicker.Resources>
                                    </DatePicker>
                                </StackPanel>

                                <!-- Row 2: Giới tính & CCCD -->
                                <StackPanel Grid.Row="2" Grid.Column="0">
                                    <TextBlock Text="Giới tính *" FontSize="13" Foreground="#374151" FontWeight="SemiBold" Margin="0,0,0,8"/>
                                    <ComboBox x:Name="CbGioiTinh" 
                                              Height="40" 
                                              FontSize="14"
                                              BorderBrush="#D1D5DB" BorderThickness="1">
                                        <ComboBoxItem Content="Nam"/>
                                        <ComboBoxItem Content="Nữ"/>
                                        <ComboBoxItem Content="Khác"/>
                                        <ComboBox.Style>
                                            <Style TargetType="ComboBox">
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="ComboBox">
                                                            <Grid>
                                                                <ToggleButton x:Name="toggleButton"
                                                                            Grid.Column="2"
                                                                            Focusable="False"
                                                                            IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                                                            ClickMode="Press"
                                                                            Background="White"
                                                                            BorderBrush="{TemplateBinding BorderBrush}"
                                                                            BorderThickness="{TemplateBinding BorderThickness}">
                                                                    <ToggleButton.Style>
                                                                        <Style TargetType="ToggleButton">
                                                                            <Setter Property="Template">
                                                                                <Setter.Value>
                                                                                    <ControlTemplate TargetType="ToggleButton">
                                                                                        <Border Background="{TemplateBinding Background}"
                                                                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                                                                CornerRadius="8">
                                                                                            <Grid>
                                                                                                <Grid.ColumnDefinitions>
                                                                                                    <ColumnDefinition Width="*"/>
                                                                                                    <ColumnDefinition Width="30"/>
                                                                                                </Grid.ColumnDefinitions>
                                                                                                <ContentPresenter Grid.Column="0" 
                                                                                                                Margin="12,0,0,0"
                                                                                                                VerticalAlignment="Center"
                                                                                                                HorizontalAlignment="Left"/>
                                                                                                <Path Grid.Column="1"
                                                                                                      Data="M 0 0 L 4 4 L 8 0 Z"
                                                                                                      Fill="#6B7280"
                                                                                                      HorizontalAlignment="Center"
                                                                                                      VerticalAlignment="Center"/>
                                                                                            </Grid>
                                                                                        </Border>
                                                                                    </ControlTemplate>
                                                                                </Setter.Value>
                                                                            </Setter>
                                                                        </Style>
                                                                    </ToggleButton.Style>
                                                                </ToggleButton>
                                                                <ContentPresenter x:Name="contentPresenter"
                                                                                Margin="12,0,30,0"
                                                                                VerticalAlignment="Center"
                                                                                HorizontalAlignment="Left"
                                                                                IsHitTestVisible="False"
                                                                                Content="{TemplateBinding SelectionBoxItem}"/>
                                                                <Popup x:Name="PART_Popup"
                                                                       Placement="Bottom"
                                                                       IsOpen="{TemplateBinding IsDropDownOpen}"
                                                                       AllowsTransparency="True"
                                                                       Focusable="False"
                                                                       PopupAnimation="Slide">
                                                                    <Border Background="White"
                                                                            BorderBrush="#E5E7EB"
                                                                            BorderThickness="1"
                                                                            CornerRadius="8"
                                                                            Margin="0,5,0,0">
                                                                        <Border.Effect>
                                                                            <DropShadowEffect Color="#000000" BlurRadius="10" ShadowDepth="0" Opacity="0.1"/>
                                                                        </Border.Effect>
                                                                        <ScrollViewer Margin="4">
                                                                            <ItemsPresenter/>
                                                                        </ScrollViewer>
                                                                    </Border>
                                                                </Popup>
                                                            </Grid>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style>
                                        </ComboBox.Style>
                                    </ComboBox>
                                </StackPanel>

                                <StackPanel Grid.Row="2" Grid.Column="2">
                                    <TextBlock Text="CCCD/CMND" FontSize="13" Foreground="#374151" FontWeight="SemiBold" Margin="0,0,0,8"/>
                                    <TextBox x:Name="TxtCCCD" 
                                             Height="40" Padding="12,0"
                                             FontSize="14" 
                                             BorderBrush="#D1D5DB" BorderThickness="1">
                                        <TextBox.Style>
                                            <Style TargetType="TextBox">
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="TextBox">
                                                            <Border Background="White" 
                                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                                    CornerRadius="8">
                                                                <ScrollViewer x:Name="PART_ContentHost" 
                                                                            Margin="{TemplateBinding Padding}"
                                                                            VerticalAlignment="Center"/>
                                                            </Border>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                                <Style.Triggers>
                                                    <Trigger Property="IsFocused" Value="True">
                                                        <Setter Property="BorderBrush" Value="#2563EB"/>
                                                        <Setter Property="BorderThickness" Value="2"/>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBox.Style>
                                    </TextBox>
                                </StackPanel>

                                <!-- Row 3: Điện thoại & Email -->
                                <StackPanel Grid.Row="4" Grid.Column="0">
                                    <TextBlock Text="Điện thoại *" FontSize="13" Foreground="#374151" FontWeight="SemiBold" Margin="0,0,0,8"/>
                                    <TextBox x:Name="TxtDienThoai" 
                                             Height="40" Padding="12,0"
                                             FontSize="14" 
                                             BorderBrush="#D1D5DB" BorderThickness="1">
                                        <TextBox.Style>
                                            <Style TargetType="TextBox">
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="TextBox">
                                                            <Border Background="White" 
                                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                                    CornerRadius="8">
                                                                <ScrollViewer x:Name="PART_ContentHost" 
                                                                            Margin="{TemplateBinding Padding}"
                                                                            VerticalAlignment="Center"/>
                                                            </Border>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                                <Style.Triggers>
                                                    <Trigger Property="IsFocused" Value="True">
                                                        <Setter Property="BorderBrush" Value="#2563EB"/>
                                                        <Setter Property="BorderThickness" Value="2"/>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBox.Style>
                                    </TextBox>
                                </StackPanel>

                                <StackPanel Grid.Row="4" Grid.Column="2">
                                    <TextBlock Text="Email" FontSize="13" Foreground="#374151" FontWeight="SemiBold" Margin="0,0,0,8"/>
                                    <TextBox x:Name="TxtEmail" 
                                             Height="40" Padding="12,0"
                                             FontSize="14" 
                                             BorderBrush="#D1D5DB" BorderThickness="1">
                                        <TextBox.Style>
                                            <Style TargetType="TextBox">
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="TextBox">
                                                            <Border Background="White" 
                                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                                    CornerRadius="8">
                                                                <ScrollViewer x:Name="PART_ContentHost" 
                                                                            Margin="{TemplateBinding Padding}"
                                                                            VerticalAlignment="Center"/>
                                                            </Border>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                                <Style.Triggers>
                                                    <Trigger Property="IsFocused" Value="True">
                                                        <Setter Property="BorderBrush" Value="#2563EB"/>
                                                        <Setter Property="BorderThickness" Value="2"/>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBox.Style>
                                    </TextBox>
                                </StackPanel>

                                <!-- Row 4: Địa chỉ (full width) -->
                               
                            </Grid>
                        </Grid>
                    </Border>

                    <!-- THÔNG TIN CÔNG VIỆC -->
                    <Border Background="White" CornerRadius="12" Padding="25">

                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="15"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- Section Title -->
                            <TextBlock Grid.Row="0" Text="Thông tin công việc" 
                                       FontSize="16" FontWeight="SemiBold" Foreground="#1F2937"/>

                            <!-- Form Grid -->
                            <Grid Grid.Row="2">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="20"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="15"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="15"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <!-- Row 1: Phòng ban & Chức vụ -->
                                <StackPanel Grid.Row="0" Grid.Column="0">
                                    <TextBlock Text="Phòng ban *" FontSize="13" Foreground="#374151" FontWeight="SemiBold" Margin="0,0,0,8"/>
                                    <ComboBox x:Name="CbPhongBan" 
                                              Height="40" 
                                              FontSize="14"
                                              BorderBrush="#D1D5DB" BorderThickness="1">
                                        <ComboBoxItem Content="IT"/>
                                        <ComboBoxItem Content="Kinh doanh"/>
                                        <ComboBoxItem Content="Marketing"/>
                                        <ComboBoxItem Content="Nhân sự"/>
                                        <ComboBoxItem Content="Tài chính"/>
                                    </ComboBox>
                                </StackPanel>

                                <StackPanel Grid.Row="0" Grid.Column="2">
                                    <TextBlock Text="Chức vụ *" FontSize="13" Foreground="#374151" FontWeight="SemiBold" Margin="0,0,0,8"/>
                                    <ComboBox x:Name="CbChucVu" 
                                              Height="40" 
                                              FontSize="14"
                                              BorderBrush="#D1D5DB" BorderThickness="1">
                                        <ComboBoxItem Content="Giám đốc"/>
                                        <ComboBoxItem Content="Trưởng phòng"/>
                                        <ComboBoxItem Content="Phó phòng"/>
                                        <ComboBoxItem Content="Nhân viên"/>
                                    </ComboBox>
                                </StackPanel>

                                <!-- Row 2: Ngày vào làm & Trạng thái -->
                                <StackPanel Grid.Row="2" Grid.Column="0">
                                    <TextBlock Text="Ngày vào làm *" FontSize="13" Foreground="#374151" FontWeight="SemiBold" Margin="0,0,0,8"/>
                                    <DatePicker x:Name="DpNgayVaoLam" 
                                                Height="40" 
                                                FontSize="14"
                                                BorderBrush="#D1D5DB" BorderThickness="1"/>
                                </StackPanel>

                                <StackPanel Grid.Row="2" Grid.Column="2">
                                    <TextBlock Text="Trạng thái *" FontSize="13" Foreground="#374151" FontWeight="SemiBold" Margin="0,0,0,8"/>
                                    <ComboBox x:Name="CbTrangThai" 
                                              Height="40" 
                                              FontSize="14"
                                              BorderBrush="#D1D5DB" BorderThickness="1">
                                        <ComboBoxItem Content="Đang làm" IsSelected="True"/>
                                        <ComboBoxItem Content="Thử việc"/>
                                        <ComboBoxItem Content="Nghỉ phép"/>
                                        <ComboBoxItem Content="Nghỉ việc"/>
                                    </ComboBox>
                                </StackPanel>
                            </Grid>
                        </Grid>
                    </Border>
                </StackPanel>
            </ScrollViewer>

            <!-- ✅ FOOTER -->
            <Border Grid.Row="2" BorderBrush="#E5E7EB" BorderThickness="0,1,0,0" 
                    Background="White" CornerRadius="0,0,16,16" Padding="30,20">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="BtnSave" Content="💾 Lưu thông tin" 
                            Background="#10B981" Foreground="White"
                            BorderThickness="0" Padding="25,12" FontSize="14" FontWeight="SemiBold"
                            Cursor="Hand" Margin="0,0,10,0"
                            Click="BtnSave_Click">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" 
                                                    CornerRadius="8" Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#059669"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                    <Button Content="Hủy" 
                            Background="White" Foreground="#1F2937"
                            BorderBrush="#D1D5DB" BorderThickness="1"
                            Padding="25,12" FontSize="14" FontWeight="SemiBold"
                            Cursor="Hand"
                            Click="BtnClose_Click">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" 
                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                    CornerRadius="8" Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#F3F4F6"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>