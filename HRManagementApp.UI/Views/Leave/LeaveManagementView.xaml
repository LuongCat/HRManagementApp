<UserControl x:Class="HRManagementApp.UI.Views.Leave.LeaveManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:HRManagementApp.Converters"
             Background="#F5F7FA">

    <UserControl.Resources>
        <converters:LeaveStatusBackgroundConverter x:Key="BgConverter"/>
        <converters:LeaveStatusForegroundConverter x:Key="FgConverter"/>
        
        <Style x:Key="TabButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#6B7280"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="20" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,25">
            <TextBlock Text="Quản lý Đơn từ" FontSize="20" FontWeight="Bold" Foreground="#1F2937"/>
            <TextBlock Text="Quản lý các đơn xin nghỉ, công tác, làm thêm giờ" FontSize="14" Foreground="#6B7280" Margin="0,5,0,0"/>
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,0,0,30">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="White" CornerRadius="12" Padding="20">
                <Border.Effect><DropShadowEffect Color="#E5E7EB" BlurRadius="10" ShadowDepth="2" Opacity="0.4"/></Border.Effect>
                <Grid>
                    <StackPanel>
                        <TextBlock Text="Tổng đơn" FontSize="13" Foreground="#6B7280"/>
                        <TextBlock x:Name="TxtTotal" Text="0" FontSize="24" FontWeight="Bold" Foreground="#2563EB" Margin="0,5,0,0"/>
                    </StackPanel>
                    <Border HorizontalAlignment="Right" VerticalAlignment="Center" Width="40" Height="40" Background="#EFF6FF" CornerRadius="8">
                        <TextBlock Text="📄" FontSize="20" Foreground="#2563EB" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </Grid>
            </Border>

            <Border Grid.Column="2" Background="White" CornerRadius="12" Padding="20">
                <Border.Effect><DropShadowEffect Color="#E5E7EB" BlurRadius="10" ShadowDepth="2" Opacity="0.4"/></Border.Effect>
                <Grid>
                    <StackPanel>
                        <TextBlock Text="Chờ duyệt" FontSize="13" Foreground="#6B7280"/>
                        <TextBlock x:Name="TxtPending" Text="0" FontSize="24" FontWeight="Bold" Foreground="#D97706" Margin="0,5,0,0"/>
                    </StackPanel>
                    <Border HorizontalAlignment="Right" VerticalAlignment="Center" Width="40" Height="40" Background="#FFF7ED" CornerRadius="8">
                        <TextBlock Text="🕒" FontSize="20" Foreground="#D97706" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </Grid>
            </Border>

            <Border Grid.Column="4" Background="White" CornerRadius="12" Padding="20">
                <Border.Effect><DropShadowEffect Color="#E5E7EB" BlurRadius="10" ShadowDepth="2" Opacity="0.4"/></Border.Effect>
                <Grid>
                    <StackPanel>
                        <TextBlock Text="Đã duyệt" FontSize="13" Foreground="#6B7280"/>
                        <TextBlock x:Name="TxtApproved" Text="0" FontSize="24" FontWeight="Bold" Foreground="#059669" Margin="0,5,0,0"/>
                    </StackPanel>
                    <Border HorizontalAlignment="Right" VerticalAlignment="Center" Width="40" Height="40" Background="#ECFDF5" CornerRadius="8">
                        <TextBlock Text="✓" FontSize="20" Foreground="#059669" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </Grid>
            </Border>

            <Border Grid.Column="6" Background="White" CornerRadius="12" Padding="20">
                <Border.Effect><DropShadowEffect Color="#E5E7EB" BlurRadius="10" ShadowDepth="2" Opacity="0.4"/></Border.Effect>
                <Grid>
                    <StackPanel>
                        <TextBlock Text="Từ chối" FontSize="13" Foreground="#6B7280"/>
                        <TextBlock x:Name="TxtRejected" Text="0" FontSize="24" FontWeight="Bold" Foreground="#DC2626" Margin="0,5,0,0"/>
                    </StackPanel>
                    <Border HorizontalAlignment="Right" VerticalAlignment="Center" Width="40" Height="40" Background="#FEF2F2" CornerRadius="8">
                        <TextBlock Text="✕" FontSize="20" Foreground="#DC2626" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </Grid>
            </Border>
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,20">
            <Border Background="#E5E7EB" CornerRadius="20" Padding="4">
                <StackPanel Orientation="Horizontal">
                    <Button x:Name="BtnTabAll" Content="Tất cả đơn" Tag="All"
                            Style="{StaticResource TabButtonStyle}" Click="Tab_Click" 
                            Background="White" Foreground="Black"/>
                    
                    <Button x:Name="BtnTabPending" Content="Chờ duyệt" Tag="Pending"
                            Style="{StaticResource TabButtonStyle}" Click="Tab_Click"/>
                    
                    <Button x:Name="BtnTabTypes" Content="Loại đơn" Tag="Types"
                            Style="{StaticResource TabButtonStyle}" Click="Tab_Click"/>
                </StackPanel>
            </Border>
        </StackPanel>

        <Border Grid.Row="3" Background="White" CornerRadius="12" Padding="25">
            <Border.Effect>
                <DropShadowEffect Color="#E0E0E0" BlurRadius="10" ShadowDepth="2" Opacity="0.3"/>
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,20">
                    <TextBlock x:Name="TxtListTitle" Text="Danh sách đơn từ" FontSize="16" FontWeight="SemiBold" Foreground="#4B5563" VerticalAlignment="Center"/>
                    
                    <Button x:Name="BtnAction" 
                            Content="+ Tạo đơn mới" HorizontalAlignment="Right"
                            Background="#111827" Foreground="White" FontWeight="SemiBold"
                            Padding="15,8" BorderThickness="0" Cursor="Hand"
                            Click="BtnCreate_Click">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="6"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </Grid>

                <DataGrid x:Name="DgRequests" Grid.Row="1" AutoGenerateColumns="False"
                          IsReadOnly="True" CanUserAddRows="False"
                          GridLinesVisibility="Horizontal" HeadersVisibility="Column"
                          Background="White" BorderThickness="0" RowHeight="55"
                          HorizontalGridLinesBrush="#F3F4F6" VerticalScrollBarVisibility="Hidden">
                    
                    <DataGrid.Resources>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Foreground" Value="#111827"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="Padding" Value="10,0"/>
                            <Setter Property="Height" Value="40"/>
                            <Setter Property="BorderThickness" Value="0,0,0,1"/>
                            <Setter Property="BorderBrush" Value="#E5E7EB"/>
                        </Style>
                        <Style TargetType="DataGridCell">
                            <Setter Property="Padding" Value="10,0"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="DataGridCell">
                                        <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                                            <ContentPresenter VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </DataGrid.Resources>

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Mã đơn" Binding="{Binding MaDonDisplay}" Width="80" FontWeight="Medium"/>
                        <DataGridTextColumn Header="Nhân viên" Binding="{Binding HoTenNhanVien}" Width="150"/>
                        <DataGridTemplateColumn Header="Loại đơn" Width="130">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="📄" Margin="0,0,5,0" Foreground="#6B7280"/>
                                        <TextBlock Text="{Binding TenLoaiDon}"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="Từ ngày" Binding="{Binding NgayBatDau, StringFormat=dd/MM/yyyy}" Width="110"/>
                        <DataGridTextColumn Header="Đến ngày" Binding="{Binding NgayKetThuc, StringFormat=dd/MM/yyyy}" Width="110"/>
                        <DataGridTextColumn Header="Số ngày" Binding="{Binding SoNgayDisplay}" Width="80" Foreground="#6B7280"/>
                        <DataGridTextColumn Header="Lý do" Binding="{Binding LyDo}" Width="*"/>
                        <DataGridTextColumn Header="Người duyệt" Binding="{Binding NguoiDuyet}" Width="130" FontWeight="Medium"/>

                        <DataGridTemplateColumn Header="Trạng thái" Width="120">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border Background="{Binding TrangThai, Converter={StaticResource BgConverter}}"
                                            CornerRadius="12" Padding="12,4" HorizontalAlignment="Left">
                                        <TextBlock Text="{Binding TrangThai}" FontSize="11" FontWeight="Bold"
                                                   Foreground="{Binding TrangThai, Converter={StaticResource FgConverter}}" 
                                                   HorizontalAlignment="Center"/>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTemplateColumn Header="Thao tác" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Width="28" Height="28" Margin="0,0,8,0" Background="White" BorderBrush="#10B981" BorderThickness="1"
                                                Cursor="Hand" ToolTip="Duyệt" Tag="{Binding MaDon}" Click="BtnApprove_Click">
                                            <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="14"/></Style></Button.Resources>
                                            <TextBlock Text="✓" Foreground="#10B981" FontWeight="Bold"/>
                                        </Button>
                                        <Button Width="28" Height="28" Background="White" BorderBrush="#EF4444" BorderThickness="1"
                                                Cursor="Hand" ToolTip="Từ chối" Tag="{Binding MaDon}" Click="BtnReject_Click">
                                            <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="14"/></Style></Button.Resources>
                                            <TextBlock Text="✕" Foreground="#EF4444" FontSize="10" FontWeight="Bold"/>
                                        </Button>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>

                <DataGrid x:Name="DgPendingRequests" Grid.Row="1" Visibility="Collapsed"
                          AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False"
                          GridLinesVisibility="Horizontal" HeadersVisibility="Column"
                          Background="White" BorderThickness="0" RowHeight="55"
                          HorizontalGridLinesBrush="#F3F4F6" VerticalScrollBarVisibility="Hidden">
                    
                    <DataGrid.Resources>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Foreground" Value="#111827"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="Padding" Value="10,0"/>
                            <Setter Property="Height" Value="40"/>
                            <Setter Property="BorderThickness" Value="0,0,0,1"/>
                            <Setter Property="BorderBrush" Value="#E5E7EB"/>
                        </Style>
                        <Style TargetType="DataGridCell">
                            <Setter Property="Padding" Value="10,0"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="DataGridCell">
                                        <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                                            <ContentPresenter VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </DataGrid.Resources>

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Mã đơn" Binding="{Binding MaDonDisplay}" Width="80" FontWeight="Medium"/>
                        <DataGridTextColumn Header="Nhân viên" Binding="{Binding HoTenNhanVien}" Width="160"/>
                        <DataGridTemplateColumn Header="Loại đơn" Width="140">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="📄" Margin="0,0,5,0" Foreground="#6B7280"/>
                                        <TextBlock Text="{Binding TenLoaiDon}"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="Từ ngày" Binding="{Binding NgayBatDau, StringFormat=dd/MM/yyyy}" Width="120"/>
                        <DataGridTextColumn Header="Đến ngày" Binding="{Binding NgayKetThuc, StringFormat=dd/MM/yyyy}" Width="120"/>
                        <DataGridTextColumn Header="Lý do" Binding="{Binding LyDo}" Width="220"/>
                        <DataGridTextColumn Header="Ngày gửi" Binding="{Binding NgayGui, StringFormat=dd/MM/yyyy}" Width="120" Foreground="#6B7280"/>

                        <DataGridTemplateColumn Header="Thao tác" Width="220">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Width="80" Height="30" Margin="0,0,8,0" Background="White" BorderBrush="#10B981" BorderThickness="1"
                                                Cursor="Hand" Tag="{Binding MaDon}" Click="BtnApprove_Click">
                                            <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="15"/></Style></Button.Resources>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="✓" Foreground="#10B981" FontWeight="Bold" Margin="0,0,5,0"/>
                                                <TextBlock Text="Duyệt" Foreground="#10B981" FontSize="12"/>
                                            </StackPanel>
                                        </Button>
                                        
                                        <Button Width="80" Height="30" Background="White" BorderBrush="#EF4444" BorderThickness="1"
                                                Cursor="Hand" Tag="{Binding MaDon}" Click="BtnReject_Click">
                                            <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="15"/></Style></Button.Resources>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="✕" Foreground="#EF4444" FontWeight="Bold" Margin="0,0,5,0"/>
                                                <TextBlock Text="Từ chối" Foreground="#EF4444" FontSize="12"/>
                                            </StackPanel>
                                        </Button>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>

                <DataGrid x:Name="DgLeaveTypes" Grid.Row="1" Visibility="Collapsed"
                          AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False"
                          GridLinesVisibility="Horizontal" HeadersVisibility="Column"
                          Background="White" BorderThickness="0" RowHeight="55"
                          HorizontalGridLinesBrush="#F3F4F6">
                    
                    <DataGrid.Resources>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Foreground" Value="#111827"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="Padding" Value="10,0"/>
                            <Setter Property="Height" Value="40"/>
                            <Setter Property="BorderThickness" Value="0,0,0,1"/>
                            <Setter Property="BorderBrush" Value="#E5E7EB"/>
                        </Style>
                        <Style TargetType="DataGridCell">
                            <Setter Property="Padding" Value="10,0"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="DataGridCell">
                                        <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                                            <ContentPresenter VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </DataGrid.Resources>

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Tên loại đơn" Binding="{Binding TenLoaiDon}" Width="*"/>
                        <DataGridTextColumn Header="Mô tả" Binding="{Binding MoTa}" Width="*"/>
                        <DataGridTextColumn Header="Có Lương" Binding="{Binding CoLuong}" Width="*"/>
                        <DataGridTemplateColumn Header="Thao tác" Width="150">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Content="Sửa" Background="White" BorderBrush="#E5E7EB" BorderThickness="1" 
                                        Padding="10,4" Margin="0,0,5,0" Cursor="Hand"
                                        Tag="{Binding}" Click="BtnEditType_Click"/>

                                        <Button Content="Xóa" Background="White" BorderBrush="#FEE2E2" BorderThickness="1" 
                                        Foreground="Red" Padding="10,4" Cursor="Hand"
                                        Tag="{Binding MaLoaiDon}" Click="BtnDeleteType_Click"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>

            </Grid>
        </Border>
    </Grid>
</UserControl>